"""shell zone.  The six room planes: floor, ceiling, the four walls and the bath box.

Empty on purpose: a later agent puts per object replacements here.  apply()
receives the Blender scene and the build context (see zones/__init__.py).
Anything placed here that no sheet or photograph fixes must be labelled
SCALED or STYLIZED in a comment beside it.
"""


def apply(scene, ctx):
    return None
