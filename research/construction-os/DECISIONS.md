# Platform build decisions (Austin's rulings)

Running log. Each entry is law until Austin overrides it in writing.

| # | Date | Decision |
|---|---|---|
| D1 | 2026-08-15 | Status workflow v1: tap-to-check with initials + timestamp stays the heartbeat on every item, plus the issue flag. Procurement statuses (Not Ordered, Ordered, In Transit, Received, Staged, Installing, Installed Complete, Damaged) turn on only for order-tracked categories (FF&E families, appliances, bath accessories, patio, pool). Other categories can enable them later via a per-category switch, no rebuild. |
| D2 | 2026-08-15 | Common areas: the 66 spaces seeded from the plans carry over as the confirmed list. Additions/archives happen in-app later. |
| D3 | 2026-08-15 | Populating categories beyond the database scope (cameras/low voltage, pool equipment, patio furniture, kitchen equipment, signage): both paths. Mine plans + submittals in Drive where they cover it (sheet-cited, flagged unknowns, never guessed) AND ship a fast in-app add-item intake flow for field fill-in. The database stays the source of truth where it has the data. |
| D4 | 2026-08-15 | Design direction: **Command Deck** (of three pitched: Command Deck, Sheet Set, Night Shift). Print-calm office command center (dark sidebar, white field, big tabular numerals, title-block accents) + dark glare-proof crew phone app. Cyan #02A9DE rationed to decisions: active nav, primary actions, spec links. This is the reference look for every screen mock and every built screen. |
| D5 | 2026-08-17 | Mock set approved as reference. Build starts with a vertical slice: **3 guest rooms on floor 1 at 100%** — rooms 101, 103, 105 (all carry their own correct 3D geometry; 101/103 connecting pair). Full real checklists for each room (FF&E and MEP punch) with live field state, initials redacted to initials-only in anything committed. |
| D6 | 2026-08-17 | Press-and-hold must NEVER pop text selection (bug in the first build). All interactive checklist surfaces ship with user-select none and touch-callout none; long-press is a deliberate gesture. |
| D7 | 2026-08-17 | **3D BIM gets its own entry in the left section menu.** Every room shows its CORRECT per-room geometry exactly like the first build (A555 dims, mirroring by reflection, connecting-door logic). Rooms without their own model (King family) keep the honest hard-stop, never a relabeled shell. |
| D8 | 2026-08-17 | Firebase signup deferred until Austin reviews the cost math (brief delivered). Slice builds on a local store shaped like the Firestore API so the backend drops in without rework. |
| D9 | 2026-08-18 | Cloud setup done, in the EXISTING Firebase project (H2SEP Checklist, `h2sep-checklist`), which Austin's account already owns: Blaze plan active, $25 budget alert set, Email/Password auth enabled (Anonymous kept for the crew app), Storage bucket created. Platform data lives in its own fenced collection `projects/h2sep/platform_rooms` (rules published by Austin 2026-08-18; deletes blocked; crew `rooms` untouched, verified by updateTime). Slice seeded and verified: 101/103/105 + MEP docs, 308 items total. Rollout stays HELD pending Austin's slice verdict. |
| D10 | 2026-08-18 | **Guestroom MEP punch condensed per Austin, flags closed by his ruling.** Mechanical = 4 lines (PTAC, thermostat, room grille, bath exhaust grille; his exact spec). Plumbing = his keep/remove markup: kept WC, lavatory faucet, shower, shower head, shower enclosure, floor drain, trap guard, kitchenette sink; removed supply stop, lav bowl, reject-check line, anti-scald valve, gate valves, PTAC condensate. Fire Protection = sprinkler heads + smoke detector (horn line removed by Austin). Electrical 5 and Low Voltage 3 are condensed drafts awaiting his marks. Every folded detail and citation kept verbatim in the item sheet; condensed lines assert nothing any open document conflict disputes. Old lines soft-deleted, never destroyed. Applies to the platform slice; crew app untouched; document conflicts remain tracked in NEEDS-CLARIFICATION. |
| D11 | 2026-08-18 | **Submittals wired into the slice, flags driven to one open question.** Per-room Drive print sheets linked in-app (10 rooms have sheets; Room 103 has none in Drive, flagged to Austin). Submittals attached to items: Danby DDW2404EBSS dishwasher (closes the 18-vs-24-inch flag; submittal governs), Moen MGXP33C disposer (closes the existence flag), Danby fridge, Danby OTR microwave, WINGITS WRD5SS36SN divider hardware, Moen vanity + kitchenette faucets and American Standard vanity + kitchenette sinks (submittal governs over plan-scheduled Delta). GR-308 working-wall naming flag closed as naming-only per Austin's remove-all-flags directive. Remaining open: GR-322 nightstand count (1 vs paper sheet's 3), question to Austin. Rollout to all rooms stays HELD until Austin approves. |
| D12 | 2026-08-18 | **Nightstand ruling: rooms with two queen beds have THREE nightstands (right, middle, left).** The room 101 paper sheet's GR-319/GR-322/GR-323 count was correct; drawings tag the family once. GR-322 line now qty 3, flag closed. **Slice is at ZERO flags.** Print sheets: the printer icon opens a live in-app sheet generated from current data (like the first build's print.html); rev 08-18 PDFs for 101/103/105 hosted under /platform/sheets/. Sheets for the other 112 rooms generate at rollout after Austin's approval. |
| D13 | 2026-08-18 | **Contacts and Assignments are live (module three: `directory`).** The real project contact list (47 people, 30 companies, from H2SEP_Contact-List_2026-08-14) is in the app: grouped by category, searchable, one tap to call or email from the field. Assignments put a company on a scope (a category) across a room set, with a role (Install or Supply), a due date, and a note; progress is counted live off the same checklist lines the crew taps, so there is no second set of books. Six assignments were auto-matched from the contact list's own Scope column (Mechanical, Electrical, Plumbing, Fire Protection, Low Voltage, Appliance) and are labeled AUTO-MATCHED · CONFIRM until Austin confirms them; nothing was guessed, so the FF&E scopes sit in the Not assigned yet list. Every room's checklist now shows who owns each scope, and tapping that chip assigns it. Data lives in two docs (`_dir`, `_asg`) shaped exactly like a room doc, so the rules Austin already published cover them with NO rules change. **The contact list carries personal cell numbers and emails, so it lives in Firestore only and never in the public repo.** Records archive, never delete. Zero data loss verified: all six room docs unchanged (same updateTime, same item counts) before and after. |
| D14 | 2026-08-19 | **The Google Sheet is the source of truth for contacts, and it syncs itself.** `tools/appsscript/SyncContacts.gs` lives inside the contact list spreadsheet: hourly plus an on-demand menu item, it pushes the sheet into `_dir` and every open app picks it up in seconds. It writes only changed fields, archives rows deleted from the sheet instead of destroying them, never touches contacts added inside the app, and **never overwrites a contact edited in the app since the last sync** (it reports the disagreement so Austin picks the winner). Sign-in is the same anonymous sign-in the crew app uses, so there is no service account, no key file, and nothing to install on his work computer. `tools/sync_directory.mjs` is the same logic on the command line for audits and restores. First sync applied Austin's two additions (Hilton, Vibrant Services) and his removal of Edward De La Garza's phone number. His in-app edit of the Texas Fire scope to "Fire Protection/Fire Sprinkler" was held, because the sheet still reads "Fire protection". |
| D15 | 2026-08-19 | **A check now records the company, not just the initials.** The identity sheet asks who you work for, offering every company in the directory (and matching it automatically when your name is already in the project directory). The company travels inside the same atomic check group as the initials and the timestamp, so the record says who signed the line and for which outfit. It shows on the checklist line, in the item sheet, in Activity, and as a SIGNED BY legend on the printed sheet. Checks carried over from paper keep their initials and simply show no company, never a guessed one. |
| D16 | 2026-08-19 | **Incident, fixed: a merge write wiped 46 contacts in the cloud.** Saving a contact edit in the app called `createIfMissing`, which used `setDoc(..., {merge: true})` with an empty `items: {}` in the skeleton. An empty map is a leaf in Firestore's merge field mask, so it wrote the empty map OVER the live one, and the only record left was the one being saved. Found within the hour by the sync tool's diff. Fixed at the cause: `createIfMissing` now reads first and writes only into a genuinely empty slot, and `ensureDoc` returns early whenever a copy already exists. Both guards are covered by a regression test. Restored from the sheet, 49 contacts verified, Austin's in-app edit preserved. No room, punch, or assignment data was touched; every room doc kept its exact updateTime through the whole episode. Standing lesson: never merge a skeleton over a document that can already hold records. |
| D17 | 2026-08-20 | **The blind critic judges the 3D render against real photographs of the demo room.** No perspective view or rendering exists anywhere in the project: A550, A551, A552, A555 and all 41 RK Hospitality ID sheets are orthographic plans, RCPs, finish plans, electrical plans and flat wall elevations, confirmed by OCR of the sheet images and the ID-0.0 drawing index. The only real-world images are the 25 photos in Drive folder '8. Photos of Demo Room' (2026-08-12), and the Drive connector returns no bytes for JPEGs, so Austin is sending them directly. Until they arrive the 3D work is checked against the A550 and A555 interior elevations at a matched orthographic camera, which tests correctness (right item, right count, right place, right size) rather than photorealism. |
| D18 | 2026-08-20 | **Floor-1 common areas use the PLAN numbering** from the architectural drawings (the reference database), not the live QC Deficiency Tracker numbering. The two disagree on 22 of 28 spaces (Lobby 003 not 001, Reception 004 not 002, Sales 022 not 003, Fitness 023 not 005, and so on). The drawings govern. The tracker's numbers are a field artifact; reconciling the crew's paperwork to the plan numbers is a separate cutover task, not a reason to seed the wrong number. |
| D19 | 2026-08-20 | **Room 118 (King Studio Acc.) gets the ROLL-IN SHOWER package, not a tub.** A554 and A103 draw a roll-in, and the FF&E Installation 1st Floor tab carries 16 shower bases (6034-L) and 16 shower doors (NOVA-6076) with no tub line for 16 rooms. A533's 'accessible tub' text is superseded. The eleven mutually exclusive tub rows in the database (BT-1 bathtub, 2 inch tub waste, HD-05 bowed rod) are dropped for 118; the roll-in rows (SH-1/SH-3 pan, kn 10 diverter) govern. Room 118 is unblocked. |
| D20 | 2026-08-20 | **GR-202 Nightstand Sconce is 2 per King room, 16 on floor 1.** The FF&E Installation 1st Floor tab reads 33, which is the value the 2nd, 3rd and 4th floor tabs carry and is impossible on a 16 key floor where every comparable line reads 15 or 16. The database gives the King Studio two nightstand sconces per room across the 8 King family rooms, which lands on 16. The sheet's 33 is a copy-down error. |
| D21 | 2026-08-20 | **The demo room is ROOM 110, a King Studio, and its 24 photographs are the comparison target.** Austin confirmed the number; two photographs independently show '110' hand written in pink marker on the corridor wallpaper beside the door. The photographs were delivered in chat, not through Drive (the Drive connector returns no bytes for JPEGs), and were recovered at full 2000x1500 resolution from the session transcript to /tmp/demo-room, so that fresh-context critic agents can open them. **They stay out of this public repo**: they are photographs of a client's unopened hotel. The written record of what they show is at research/king-studio/demo-room-observations.md. Room 110 is one of the six plain King Studios (104, 106, 108, 110, 112, 114), so the geometry and package derived from these photographs apply to the whole King Studio type. |
| D22 | 2026-08-20 | **The plain Queen-Queen working wall is GR-305, not GR-308. This supersedes D11 on that line.** Austin: "fix GR-305 on 105-115 and regenerate 105". The FF&E Installation workbook's 1st Floor tab carries the two as separate purchased parts with separate counts: GR-305 Working Wall @ QQ at L 2 plus R 4 = 6 units, and GR-308 Working Wall @ QQ Connector at L 1 plus R 1 = 2 units. Floor 1 has exactly 6 plain Queen-Queen keys (105 107 109 111 113 115) and exactly 2 QQ connecting keys (101, 103). Six and two, no remainder. `data/project.sqlite` carries NO GR-305 row anywhere in the building: every QQ room was transcribed as GR-308, and the transcriber flagged the oddity in the row itself. D11 closed that flag as a naming quirk with no order impact. D11 was decided without the workbook and was wrong, because two different parts were actually purchased; a crew told to install GR-308 in room 109 would hang the wrong casework. **Still open, deliberately not guessed:** the workbook splits GR-305 into 2 LEFT and 4 RIGHT across those six rooms and no document states which room takes which hand, so the line ships as the base tag at reliability MEDIUM with the handedness called out. Confirm with RK Design before the casework is released. **Room 105 was regenerated** using a new `--regen` path that is create-only by default and cannot destroy: the old gr308_a line is kept as a tombstone with `deleted: true` rather than dropped, so any check-off already recorded against it survives; all 77 of 105-MEP's deleted history rows were carried over untouched; every other field that moved is reported line by line. Its sprinkler head count moved from 1 to 3 so it stops disagreeing with the five other rooms of its own type. Zero keys lost, and rooms 101 and 103 stayed byte-identical. The generator selftest was bypassing the correction, so it now walks the same path the build walks and expects exactly the ruled difference. **Migration note for cutover:** live room 105 still holds gr308_a. The rollout must soft-delete it and add gr305_a, never drop the line. |
| D23 | 2026-08-20 | **The crew's work is carried into the floor-1 build, and a rebuild can no longer erase it.** Austin: "make sure to carry all of the checked off items notes etc. to the this build". The live crew app holds real field work on floor 1: **382 check-offs, 289 open issues and 5 notes across 19 documents**, dating from July. The staged rooms were born clean, so rolling them out as built would have wiped months of field time. `platform/tools/carry_field_state.mjs` reads the crew collection (READ ONLY, snapshot taken first) and carries checked, initials, checkedAt, checkedAtLocal, issue and issueResolved onto the matching lines. Item keys matched 624 of 629 directly, because both builds share a generator lineage. The 5 that did not are the working wall retagged under D22: the crew checked that wall off as GR-308, so the work is carried across to GR-305 as a ruled remap, and room 105's retired line keeps its original check as history rather than counting twice. **Reconciliation is exact, not approximate: 382 to 382 checks and 289 to 289 issues on live lines**, and the run fails if it is not. **PII: 755 name and account-id fields were dropped on the way in.** The seed is a public repository, so `checkedByName` and `checkedByUid` never enter it and note authors are reduced to initials ("Morgan Davis" becomes "MD"). Initials and timestamps are what the paper sheet carries and what the approved slice already carried, so that is the line. **A separate defect found and fixed while doing this:** the generator reset every check to false on rebuild, so any later `--regen` would have silently zeroed the floor. It now re-applies field state from the staged copy onto freshly built lines and reports any work whose line no longer exists. Covered by a regression check. |
| D24 | 2026-08-21 | **FLOOR 1 IS LIVE. Austin: "go ... let's push this fucking thing live: live dashboard and live app."** The rollout ran per CUTOVER-FLOOR1.md: backup first, three-way merge, read-back verify. Result: 81 platform docs (plus _dir/_asg = 83), 1,554 item records, 382 check-offs and 288 open issues live, the one conflict (101/hd08_a, resolved in the platform app) kept in the live app's favour, and the crew collection untouched (updateTimes unchanged, verified). The deployed app now seeds from the full floor-1 build with the approved slice as fallback, the sidebar reads FLOOR 1 LIVE, and MODEL_ROOMS covers the whole QQ family (101/103/105/107/109/111/113/115) since A555 geometry exists for all of them; the King family keeps the D7 hard-stop. **STANDING DIRECTIVE FOR FLOORS 2, 3 AND 4, recorded as law:** when those floors are built, ALL notes, markups, check-offs, initials, timestamps and issues from the old crew app come with them, and NOTHING is deleted - the same carry_field_state discipline used on floor 1, with exact reconciliation and PII redaction. A floor build that arrives clean is wrong by definition. Also standing: several floor 2-4 room types (King One Bedroom, King One Bedroom Acc., QQ Extended, QQ Acc.) have no approved reference room, so one reference room per type gets built and approved before the type generates at scale. |
| D25 | 2026-08-21 | **The floor-1 3D assembly is NOT built. Stopped on a usage limit, not on a decision.** Austin asked to complete the King-family models and assemble all of floor 1 into one navigable scene. The workflow was authored and launched (run wf_a6b0cb81-2e3: A100 layout recon, King geometry spec, King rooms into platform/room3d.html, an assembled platform/floor3d.html with live status paint and click-through, a dimension verifier, and three design critics). **All eight agents failed on the Fable 5 usage limit before doing any work.** Nothing was written: no floor3d.html, no geometry-spec.json, working tree clean, room-3d.html byte-identical. The plan is intact in the workflow script at `.../workflows/scripts/h2sep-floor1-3d-wf_a6b0cb81-2e3.js` and can be re-run as-is on a model with available capacity. **What DOES ship in the 3D today:** the whole QQ family (101/103/105/107/109/111/113/115) renders its own correct A555 geometry in the live app; the King family keeps the honest D7 hard-stop rather than a relabeled QQ shell; and platform/king-studio.html exists as a separate materials-and-light exhibit of the King Studio, dimensionally built to A550. |
| D26 | 2026-08-24 | **Austin approved the Electrical and Low Voltage condensed punches as drafted.** Electrical stays 5 lines (panelboard, GFCI, lights, outlets, sink switches) and Low Voltage stays 3 (WAP, TV/data/desk jacks, phone/doorbell). "Electrical - looks good ... Low voltage is good." With plumbing (D10) and mechanical already his, every trade band of the guestroom MEP punch is now owner-approved. Also ruled in the same message: GR-305 handedness stays OPEN, he will find the answer himself ("Leave this item I will find out answer later"), and the room 118 tub-mark RFI is ON HOLD ("Hold off on drafting the RFI"). |
| D27 | 2026-08-24 | **Hot and cold water verification added to every guest room MEP punch.** Austin: "Make sure to add hot and cold water work." One plumbing line per room (`plmb_hotcold_a`, sort 3018): run hot and cold at the lavatory, shower and kitchenette sink, both temperatures arrive, hot on the LEFT. Lives in the generator as a RULED_LINE_ADDITION so no rebuild can drop it. Applied to the staged seed and to all 16 LIVE rooms (backup taken first, additive create-only patch, 16/16 verified by read-back, crew collection untouched). |
| D28 | 2026-08-24 | **Door Hardware category added to every guest room FF&E checklist: closer and lock.** Austin: "add items to FF&E - 1. Door Closer installed ... 2. Door Lock Installed", with site photos of the delivered product. Identities transcribed VERBATIM from his photo labels, not guessed: closer RIXSON R21013 Series 10, UL Classified MISCELLANEOUS FIRE DOOR ACCESSORIES 2MF0; lock box label NORTON RIXSON / ASSA ABLOY 10-336, DOOR, finish 630, one unit photographed installed on a guestroom frame. Two lines per room (`dh_closer_a`, `dh_lock_a`) in a new Door Hardware band at the end of the FF&E list. Generator-carried (RULED_LINE_ADDITION), staged and LIVE on all 16 rooms, verified. The photos themselves stay out of the public repo; the notes carry the transcription. **Standing sequence he set in the same message:** deliver the FULL mockups of the four unapproved room types (King One Bedroom, King One Bedroom Acc., QQ Extended, QQ Acc.) for his approval, then complete the per-room 3D BIM models and the per-floor BIM model. |
| D29 | 2026-08-24 | **Bed skirts on the ADA rooms.** Austin: "make sure to add Bed Skirts to the ADA rooms." The database already carries GR-603.1 King Bed Skirt on all five King-family accessible keys (118, 217, 317, 417, 438) and live room 118 has the line. The real gap his direction closes is the two QQ Acc. rooms, 238 and 338: each has two GR-602.ADA ACCESSIBLE open bed bases and NO skirt row, because the standard queens use a GR-600.1 Box Spring Cover that an open ADA base cannot take, and no queen bed skirt tag exists anywhere in the document set. Ruled line: Queen Bed Skirt, code BS-Q, qty 2, one per bed, matching the room's own two GR-602.ADA rows - a derived count, not a guess. No document tag exists, so the line says so and the size gets confirmed with the FF&E supplier before ordering. Carried as a RULED_LINE_ADDITION in the generator (applies only to accessible QQ Acc. rooms; floor 1 has none, so the live floor is unchanged) and queued for injection into mockup room 238 the moment the round-3 fix workflow releases the file. |
| D30 | 2026-08-24 | **The four room-type mockups are delivered for approval, with the critic trail printed on them.** King One Bedroom (202), King One Bedroom Acc. (217), QQ Extended (230), QQ Acc. (238): full FF&E and MEP packages, crew field state carried exactly (39/39 checks, 88 issues, 3 notes, PII stripped), D27/D28 lines present, and **D29's Queen Bed Skirt (bsq_a, BS-Q, qty 2) injected on 238**. Four rounds of build-verify ran with fresh adversarial critics each round; defect trajectory 21, 26, 25, 10. Round 4 changed strategy - room-wide negative claims went from 50 to 0 and every meta claim is now enforced by platform/tools/assert_ref_claims.mjs - and the count finally broke. **The 10 remaining findings are all prose self-consistency inside line notes; none is a data defect.** No round faulted a category, tag, quantity, mark, reliability value or the carried crew state in the final version. Loop stopped by the standing no-round-5 rule; the mock book shows all four verdicts and the trajectory on its front page, so approval is made with eyes open. One brief error caught by the round-4 fix agent and worth recording: room 202 is NOT an accessible key, so it correctly carries no GR-603.1 bed skirt - D29's reach is exactly the seven accessible keys. |
| D31 | 2026-09-02 | **The floor-1 3D scene was built and judged once, then destroyed by a container restart before its checkpoint commit; the rebuild is authored and ready.** Workflow run wf_a5dcbbb1-c5e completed all eight agents: the King family 104-118 built into platform/room3d.html with all eight rooms proven (118 measured off A552, pulled from Drive), and a new platform/floor3d.html assembling all 46 floor-1 spaces with live status paint, hover cards, click-through, and Overview / Guest Wing / Common Areas / Exploded views. Judged honestly before the loss: dimension verifier FAIL on five defects (an unproven 4'-3" shift encoded as a dead constant against the spec's own do-not-encode note; a false proof narrative on room 118's partition datum while the rendered geometry was right; three non-coincident GR-3 openings at 116/118 where the QQ pair already uses one datum; the packaged artifact never inlined the floor scene so its Floor 1 screen was a silent blank; the second accessible clear-floor circle in 118 drawn differently in the two viewers with no printed size or position). All three design critics STRONG, not WOWED: two named the 30 empty common areas, one named the open-issue marker that reads as a rug instead of a red pin at the door. The container restarted in the seconds between reading that result and committing; every generated file and every agent transcript was lost. What survived is committed: research/king-studio/geometry-spec.json, research/floor1-3d/a100-layout.json (A100 measured at a proven 6.75 pt/ft with an honest gap list: no pool deck on A100, the front of house is one open volume, several back-of-house rooms have label anchors only, revision 5 clouds on both connecting pairs), research/floor1-3d/king-spec-recon.json, and platform/room3d.html force-added past its gitignore so the King build can never be lost the same way again. The rebuild workflow (research/floor1-3d/rebuild-workflow.js) bakes every defect and critic fix into the build prompts and makes the build agents commit and push their own files at every milestone. It was launched and then stopped within a minute on Austin's "ok stop give me a hand off file for a new session"; nothing was written by it. Handoff: research/construction-os/HANDOFF-2026-09-02.md. D25 stands as history: this row supersedes its "NOT built" status. |
| D32 | 2026-09-02 | **FLOOR 2 IS BUILT OUT AND STAGED FOR APPROVAL - FF&E and MEP, not the 3D. Not live.** Austin: "start here at this repo but I need the 2 floor built out. Just the FF&E & MEP not the 3d bim yet." Every floor-2 key in data/project.sqlite rooms - 33 guest rooms, 66 documents - plus the three floor-2 common areas that carry a gated line (corridor 221 with an MEP companion, elevator lobby 237, ice machine 239; the other six spaces hold only finish rows the approved gate keeps out and get no document, listed as such). Generator platform/tools/build_floor2.mjs is build_ref_rooms.mjs generalised to a floor: room table read from the database, donor chosen by type (King Studio from 104, Queen-Queen from 105, QQ Connecting from 103, the four mock-up types from the donors the mock-ups used), the reduction recipe still copied byte for byte from build_floor1.mjs and re-proved on every run, sort convention measured per donor, sprinkler lines counting a room's own head rows and dropping the first-floor total, a rebuild preserving field state and carrying crew-only lines forward. The four mock-up rooms rebuild identical to the D30 mock-ups on shape and text. **Ruling D22 applied to the eight plain Queen-Queen keys on floor 2 and to no other type,** on the same evidence standard it used on floor 1: the FF&E Installation workbook's 2nd Floor tab lists the working walls as separate purchased parts and every count reconciles against the floor's key mix with no remainder (GR-304 17 = 17 King Studios; GR-305 11 = 8 plain Queen-Queen + 1 QQ Wide + 2 QQ Extended; GR-308 2 = the 2 connecting keys; GR-309R 1 = the 1 QQ Acc.; GR-315 and GR-316 1 each = 202 and 217, which the database already tags). The eight plain rooms ship GR-305 at MEDIUM with the hand open (the tab splits them 5 LEFT, 6 RIGHT; D26 records Austin is answering the hand himself). **Deliberately NOT retagged, evidence written on the line and in note n_d22, OPEN for Austin:** 201, 230 and 232 (the tab's eleven GR-305 walls only reconcile with them counted in, but D22 names the plain type only) and 238 (GR-309R purchased, spec and ID-5.9 say GR-309, A556 tags GR-308). **The crew's work is carried exactly (D24):** 579 check-offs, 696 open issues and 28 notes across 69 crew documents, identity reconciled by platform/tools/carry_floor2.mjs, 1,186 name and account-id fields dropped, note authors reduced to initials, crew collection proven untouched by updateTime. Two crew lines the build could not produce were carried rather than lost: room 202's GR-905 (FF&E - Misc, outside the gate, rebuilt from the room's own row as on the mock-up) and room 201's field-authored line "Lamp shades are in the room in a box." (no category, no citation; carried verbatim and labelled FIELD-AUTHORED). Audit tests/floor2-audit.mjs: 51 checks, all pass, including that a rebuild keeps every check and the space docs, that two independent builds are byte-identical, and that floor1-staged.json, slice-f1.json and ref-rooms-staged.json were never written. **Where floor 2 is stricter than floor 1:** 446 lines on same-type rooms are MEDIUM or FLAGGED where the floor-1 donor line is HIGH, every one from an OPEN conflicts-table entry carried onto the line (B4.5, B4.2, B3.1, A11, B4.4) or the worst-of-own-rows MEP rule the mock-ups already follow; floor 1 was built before that rule. Listed in the review book so the difference is a decision, not a surprise. Review book: research/floor2/floor2book.html. Rollout of floor 2 waits on Austin's approval and follows the floor-1 runbook (backup, three-way merge, read-back verify, crew collection never written). |
| D33 | 2026-09-02 | **Working walls on the floor-2 two-queen keys: 201, 230 and 232 take GR-305; 238 takes GR-309.** Austin, reviewing the floor-2 book: "ok retag 201, 230, 232 to GR-305 and 238 to GR-309". Closes the two working-wall questions D32 left open. Evidence on the line, verbatim from the FF&E Installation workbook's 2nd Floor tab: GR-305 Working Wall @ QQ at L 5 + R 6 = 11 units, which reconciles only as the 8 plain Queen-Queen keys plus QQ Wide 201 plus QQ Extended 230 and 232; GR-309R Working Wall @ QQ Accessible at 1 unit against the one QQ Acc. key, with the FF&E spec and ID-5.9 also naming GR-309 while A556 tags GR-308. Carried as TAG_CORRECTIONS_F2 entries in build_floor2.mjs (same mechanism as D22), so no rebuild can drop it; the corrected lines ship at MEDIUM with the hand open (the tab prints GR-309R; whether that R is a hand is not asserted) and keep every open conflicts-table entry that names them. The crew's check-offs on all four walls, made under GR-308, came across the retag (carry_floor2.mjs, 12 lines carried across a ruled retag floor-wide). The D30 mock-ups of 230 and 238 are superseded on this one line; the audit declares the difference. 53 checks pass. Still open: the per-room LEFT/RIGHT hand on every GR-305 and on GR-309 (D26: Austin is answering it). |
| D34 | 2026-09-02 | **FLOOR 3 IS BUILT OUT AND STAGED FOR APPROVAL - FF&E and MEP, not the 3D. Not live.** Austin: "once completed lets start floor 3 just no 3d BIM yet. i need this expedited so deploy as many agents and subagents that makes sense." Same generator as floor 2, generalised to a floor argument (build_floor2.mjs --floor=3, carry_floor2.mjs --floor=3, tests/floor3-audit.mjs, the book builder with --floor=3): 33 guest rooms, 66 documents, plus the three floor-3 common areas with a gated line (corridor 321 with an MEP companion, elevator lobby 337, ice machine 339); the other six spaces hold only finish rows outside the approved gate and get no document, listed. Floor 2 was regenerated under the generalised tool and re-audited (53 checks); only the wording of two build-authored room notes moved. **The crew's floor-3 work is carried exactly (D24): 412 check-offs, 701 open issues, 10 notes across 69 crew documents**, PII dropped, crew collection proven untouched; room 302's GR-905 line rebuilt from its own row so the crew's open issue has a home. Audit: 52 checks, all pass, including that every floor-3 room matches the staged floor-2 room of its type on shape apart from the floor-2 working-wall retags, that floor-3 lines cite floor-3 sheets (A102, A122) where floor 2 cites A101 and A121, and that floor2-staged.json was never written by a floor-3 run. **NO WORKING-WALL CORRECTION ON FLOOR 3, deliberately.** D22 and D33 rest on the workbook's 2nd Floor tab reconciling exactly against floor 2's key mix. The 3rd Floor tab prints the same six figures (GR-304 17, GR-305 11, GR-308 2, GR-309R 1, GR-315 1, GR-316 1) and floor 3's key mix per the drawings is different: nine plain Queen-Queen plus one QQ Wide plus two QQ Extended is 12 two-queen walls against 11 printed, and one connecting key (336) against 2 printed. GR-304, GR-309R, GR-315 and GR-316 reconcile; GR-305 and GR-308 do not, and D20 already found a copied-down figure on these tabs. So every two-queen key on floor 3 (301, 303, 305, 307, 309, 311, 313, 315, 328, 330, 332, 334, 338) carries GR-308 exactly as transcribed, FLAGGED, with the arithmetic in room note n_d22, OPEN for Austin: whether D22 and D33 carry up by room type, or whether the drawings (is 315 a connector on A102?) or the tab need correcting first. Review book: research/floor3/floor3book.html. Recorded before the fresh-eyes verifier reports; anything it finds is fixed on top. |
| D35 | 2026-09-02 | **On floor 3, D22 and D33 carry up by room type: plain Queen-Queen, QQ Wide and QQ Extended take GR-305; QQ Acc. takes GR-309; the connecting key keeps GR-308.** Austin, reviewing the floor-3 book: "carry D22 and D33 up by room type on floor 3". Applied to 301, 303, 305, 307, 309, 311, 313, 315, 328, 330, 332, 334 (GR-305) and 338 (GR-309); 336 stands on GR-308. The evidence on each line is the ruling itself: the workbook's 3rd Floor tab prints the same six working-wall figures as the 2nd (GR-305 11, GR-308 2) and floor 3 per the drawings has twelve non-connecting two-queen keys and one connector, so the tab is one GR-305 short and one GR-308 over and is quoted on the line as NOT the evidence; GR-304 (17), GR-309R (1), GR-315 (1) and GR-316 (1) do reconcile. Lines ship at MEDIUM with the hand open (5 LEFT / 6 RIGHT printed for eleven walls where floor 3 has twelve; the twelfth unit and every hand to be confirmed with RK Design). Carried as TAG_CORRECTIONS_F2 entries scoped to floor 3; the crew's check-offs made under GR-308 came across (9 lines carried across the retag). Two corrections made on the same pass, both floors rebuilt and re-audited (floor 2: 53 checks; floor 3: 55 checks): the conflicts-table room-key matcher no longer reads a tag run like "GR-300/305/307/308" as naming rooms 305, 307 and 308 (a fresh-eyes reconnaissance agent caught it; it had been misreporting on every floor since the mock-ups), and the generator's sprinkler comment no longer calls rooms 107 and 108 floor-2 keys. The recon agent independently confirmed the floor-3 room mix, the crew totals (412 / 701 / 10), the four space documents, the tab figures and the single gate-outside crew line (302 GR-905). Still open on floor 3: the hand per wall, the bathing configuration on 317 (whose drawing-record note says "roll-in bath drawn" while nobody has ruled) and 338, the common-area finish rows, and conflicts A11 (G001's matrix has no third-floor QQ accessible while the purchase record has one GR-309R). |
| D36 | 2026-09-02 | **FLOOR 4 IS BUILT OUT AND STAGED FOR APPROVAL - FF&E and MEP, not the 3D. Not live.** Austin: "once completed lets start floor 4 just no 3d BIM yet." Same tools with --floor=4: 33 guest rooms, 66 documents, plus corridor 421 (with an MEP companion), elevator lobby 437 and ice machine 439; the other six spaces hold only finish rows outside the approved gate. Two room types no other upper floor has: **401, QQ Wide Connecting** (display label "QQ Studio Connector" per OV-001), built from the same type's approved floor-1 room 101, connecting plan references kept, GR-308 connector wall; and **438, King Studio Acc.** (display label "King Studio Accessible Connector"; the rooms table records the PM's 2026-08-12 ruling that it is the accessible connector, connecting = 1), whose package donor is King Studio 104 and NOT the live floor-1 room 118: 118 was built under ruling D19 (roll-in shower, room 118 only) and its closed bathing lines and D19-shaped numbering must not travel, so 438 carries BOTH bathing configurations, FLAGGED, like the accessible mock-ups. Room 438 resolves to sheet A551 on the database's own facts (it carries GR-502 on A551 and neither GR-320 nor GR-208, the two rows marked "present on A552 (118), absent on A551 (438)"; room_types records the ~90% assumption), the mirror of build_floor1's resolution of 118 to A552. GR-307 is 438's working wall (accessible keys take GR-307, per the PM ruling recorded in the rooms table). **The crew's floor-4 work is carried exactly: 3 check-offs, 442 open issues, 1 note**, PII dropped, crew collection proven untouched; room 402's GR-905 rebuilt from its own row so the crew's open issue has a home. Audit tests/floor4-audit.mjs: 55 checks, all pass. **NO WORKING-WALL CORRECTION ON FLOOR 4, OPEN for Austin.** No ruling names floor 4 (D22 floors 1 and 2, D33 floor 2, D35 floor 3), and the workbook's 4th Floor tab prints the same figures as the 2nd and 3rd (GR-305 11, GR-308 2, GR-309R 1) against a floor-4 mix of 8 plain Queen-Queen + 0 QQ Wide + 2 QQ Extended = 10, three connecting keys (401, 403, 436) and no QQ Acc. key: 11 against 10, 2 against 3, 1 against 0. GR-304 (17), GR-315 (1) and GR-316 (1) reconcile. So 405, 407, 409, 411, 413, 415, 428, 434, 430 and 432 carry GR-308 exactly as transcribed, FLAGGED, with the arithmetic in n_d22; the three connecting keys say GR-308 stands. If Austin carries D22, D33 and D35 up by room type, the same one-line profile change applies them. Review book: research/floor4/floor4book.html. Recorded before the fresh-eyes verifier reports on floors 3 and 4; anything they find is fixed on top. |
| D37 | 2026-09-02 | **On floor 4, D22, D33 and D35 carry up by room type: the eight plain Queen-Queens and the two QQ Extended keys (430, 432) take GR-305; the three connecting keys (401, 403, 436) keep GR-308; 438 (King Studio Acc.) keeps GR-307.** Austin, reviewing the floor-4 book: "carry D22, D33 and D35 up by room type on floor 4". Floor 4 has no QQ Acc. key, so the GR-309 half has nothing to reach. Carried as a TAG_CORRECTIONS_F2 entry scoped to floor 4; every corrected line quotes the ruling and states that the 4th Floor tab (GR-305 11, GR-308 2, GR-309R 1) does not reconcile with floor 4's mix (10 against 11, 3 against 2, 0 against 1) and is not the evidence. Lines ship at MEDIUM with the hand open (5 LEFT / 6 RIGHT printed for eleven walls where floor 4 has ten; confirm with RK Design). The crew has no check-off on any floor-4 working wall, so nothing crossed the retag. Rebuilt, carried (3 / 442 / 1 exact) and re-audited: 55 checks pass. With D37 every two-queen working wall in the building is ruled: GR-305 on every non-connecting two-queen key on floors 1 to 4, GR-308 on every connecting key, GR-309 on the two QQ Acc. keys (238, 338). Still open building-wide: the LEFT / RIGHT hand per wall (D26). |
| D38 | 2026-09-02 | **Fresh-eyes verifier findings on the floor-3 build, fixed at the mechanism and applied to floors 2, 3 and 4.** An independent adversarial agent read the floor-3 build cold and returned FAIL on three data-level items and nine prose items, with no wrong tag, count, citation, lost crew work or PII (its own words: the tag, quantity, citation and field-state layers survived every test). Fixed: (1) the staged file's meta.floor was hardcoded 2 on every floor, now the built floor; (2) on floor 3 and 4 the MEP sprinkler line said "what is left is A121" (the second-floor ceiling plan) after the whole segment had been dropped and re-pointed to A122 / A123 - a trim is now reported only when the trimmed segment survives; (3) open conflict A13 (G001's 55 sqft Queen-Queen versus QQ Wide area difference) never reached the Queen-Queen or QQ Wide rooms because the conflicts table writes "Queen Queen" and "Queen Queen (Wide)" while the matcher required the database's spelling - the type-area matcher now carries the table's own aliases, and A13 rides on 56 to 58 rooms per floor instead of 19; (4) a ruled working wall named by open conflict B4.5 said FLAGGED and MEDIUM in one note while the field read MEDIUM - it now ships FLAGGED while B4.5 is open, says the ruling alone would make it MEDIUM, and GR-309 (which B4.5 does not name) stays MEDIUM; (5) the carry tool's rebuilt-line note claimed a check-off that did not exist - it now names exactly what was carried (open issue, check-off, or both); (6) every gate-gap note named carry_ref_state.mjs as the carrier and used floor-2 room 217 as its worked example - now the floor's own carry command and a type-level example; (7) the D12 nightstand sentence said "both positions" for three; (8) a curly apostrophe and the British "relabelled" in tool prose; (9) em-dash joins in this tool's own prose replaced with hyphens (HANDOVER rule 7) - joins inside functions copied byte-for-byte from build_floor1.mjs are untouched, since changing them would fork the recipe; (10) the D35/D37 line note's duplicated subject and a missing period. Not changed, on purpose: the approved D10 lavatory faucet line's mark is an em dash on the live floor and stays so; a space MEP line with no note carries no SOURCE sentence, as on floor 1. All three floors rebuilt, carried (579 / 696 / 28; 412 / 701 / 10; 3 / 442 / 1, all exact) and re-audited (53, 55, 55 checks). The floor-2 mock-up comparison normalises the prose fixes. |
| D39 | 2026-09-02 | **Fresh-eyes verifier findings on the floor-4 build, fixed at the mechanism and applied to floors 2, 3 and 4.** A second independent adversarial agent read the floor-4 build cold. Fixed: (1) a crew note stored in the crew collection as a plain string (room 438's "rename-2026-08-12" note) arrived with empty text - the carry tool now normalizes string notes into note objects, refuses to write a note with empty text, and reconciles notes on non-empty identical text only; every floor audit gained the check "every note carries non-empty text"; (2) the D12 override sentence read "Confirm all three positions" on a qty-2 line - it is now count-aware ("both" for 2, "all N" otherwise); (3) a field-authored line restored from the crew collection kept stale prose from an earlier carry - restored lines are now re-derived on every carry at their prior sort; (4) the connecting-key working-wall note on 401 named the wrong room type - it now reads the room's own type and cites D33, D35 and D37 by floor. Also fixed as audit false positives: the room-key regex read explanatory parentheticals as keys, the type slug carried a trailing hyphen, the floor-1 twin reliability comparison, notes counted by key, and other-floor seeds compared by md5. All three floors rebuilt, carried (579 / 696 / 28; 412 / 701 / 10; 3 / 442 / 1, all exact) and re-audited (54, 56, 56 checks). Room 438's note now reads in full. Its crew-authored text names the PM by full name; crew note text is never altered by the carry (D24), so it ships as written. |
| D40 | 2026-09-02 | **Floors 2, 3 and 4 approved and rolled live for the app and dashboard.** Austin: "approve floors 2, 3 and 4 and push them live for the app and dashoard." Run as recorded in CUTOVER-FLOORS-2-4.md: every floor rebuilt, carried and audited after D39 (54, 56, 56 checks); platform/tools/rollout_floor.mjs (rollout_floor1.mjs made floor-parametric, refuses floor 1, refuses a file whose floor disagrees with the flag) dry-run then applied per floor with a backup first; 70 documents created per floor (33 rooms, 33 MEP punches, 3 spaces plus their MEP), 0 updated, zero-loss check OK, every document read back and its line, check and note counts verified, every document outside the floor proved unmoved by updateTime. The platform collection went from 83 to 293 documents; floor 1, _dir and _asg untouched. The crew collection (296 docs) was read before and after every run and never moved. The app's seed loader now merges floor1..4-staged.json, the sidebar reads FLOORS 1-4 LIVE, and the dashboard, rooms and common-areas screens take their floor range from the documents and group lists by floor; 26 of 27 floor-1 UI checks and 12 of 12 four-floor UI checks pass (the one failure is a stale local-log check that fails identically on the previous deploy). Deployed by targeted copy to main and main to gh-pages; the three seeds verified byte-identical live, the crew app's root files byte-identical by md5 (6 files) and its index.html md5 identical live. The room type note on every floor 2 to 4 room, which had read "STAGED FOR APPROVAL - NOT LIVE", now records this approval; all three floors were rebuilt, re-audited and rolled again as UPDATE runs (579 / 476, 412 / 464, 3 / 445 checks and notes unchanged live). Not resolved by going live and still open on the lines: the working-wall hand per room (D26), the bathing configuration on the six upper-floor accessible keys, conflict A11 on 338, the common-area finish rows outside the gate, and the full name in room 438's crew note (crew text never altered, D24). |
| D41 | 2026-09-02 | **Full platform build deployed; 3D BIM shown for floor 1 only, every floor-1 key modeled.** Austin: "push the full build out of the dashboard and app. Only show 3d bim for 1st floor as its already completed." Before extending the model list past the QQ family, a fresh adversarial verifier rendered all 18 floor-1 keys in platform/room3d.html and compared every King constant to research/king-studio/geometry-spec.json: all 20 KING_STD and all 25 KING_ACC constants match their citations, conflicts C1 (116 = 132 in, 118 = 132.82 in, never averaged), C2 and C3 handled, D19 roll-in built on 118, the rendered tag set on every King room identical to the room's own document in floor1-staged.json, zero page or console errors on all 16 rendering rooms, and 102 and 120 hard-stop. Six King rooms (104, 106, 108, 110, 112, 114) passed outright. Two carried the prior judgement's open defects V1 and V5 and were fixed before shipping: the dead constant wwShift = 51 in (the unproven 4 ft 3 in shift the spec forbids as a hard number) is deleted from KING_CONN, and 118's second clear-floor circle, which A552 draws with no printed diameter or position, is now drawn fainter and labelled "CLEAR FLOOR SPACE - STYLIZED - A552 PRINTS NO SIZE OR POSITION" beside the proven 60 in turning circle. Re-rendered 116, 118, 105 and 110 clean. MODEL_ROOMS is now every floor-1 key (16); the 3D BIM hub lists floor-1 rooms only and says floors 2 to 4 have no models; upper-floor rooms carry no 3D button. Recorded by the verifier and not changed: room3d.html hard-codes its tag list rather than reading floor1-staged.json, so a package change will not propagate to the viewer (it matches today, room by room); soft cross-sheet borrows on 116's GR-3 rough opening and 118's window band heights are disclosed on the cards; mirrored rooms mirror the camera and not the lights (visual only). Deployed as the complete platform folder (tools/private excluded) to main and gh-pages, crew root files byte-identical. |
| D42 | 2026-09-02 | **Bulk marking on the dashboard; the floor-1 3D scene rebuild put on hold.** Austin: "ok put the 3d bim model on hold. I need you to make it to where I can Bulk mark items on the dashboard." The rebuild workflow was stopped in its first phase and the partial scene source committed to the branch as on-hold work (not wired, not deployed). Bulk marking ships two ways on one engine: (1) Select mode on every room and space checklist (a Select lines button): tap lines or select a whole category, then Mark checked, Mark unchecked, Resolve issues, Resolve and check, or Flag an issue; (2) a Bulk mark screen in the office nav: pick where (rooms or common areas, FF&E or MEP punch, floors, room types), which lines (category, then tag), and what to do, across many rooms at once. Every apply is previewed first with the exact count and every line left alone listed with its reason, in the crew dashboard's own vocabulary: already checked off, checked by someone else, not checked, flagged so sources disagree, open issue so resolve it first. Another person's initials are field evidence and are never restamped unless the restamp switch is turned on for that apply. A flagged line or a line with an open issue is never checked off in bulk. Each apply writes ONE atomic patch per document with the same complete check-field group a single tap writes (checked, initials, company, two timestamps), records one activity line per document, and offers Undo on the toast, which writes the exact inverse. 30 browser checks in tests/bulk-ui.mjs, run in local mode so nothing can touch Firestore; the four-floor and floor-1 suites unchanged. Deployed to main and gh-pages. |
| D43 | 2026-09-02 | **Bulk marking completed under three independent reviews, all findings fixed at the mechanism and live.** Austin: "deploy as many agents and sub agents as needed to complete the bulk edit on the dashboard." Three fresh agents ran in parallel against the shipped feature: a browser attacker, a data-safety code reviewer, and a design critic. Fixed from the review: Undo re-reads every line and reverts only lines that still hold exactly what the bulk wrote (a line changed since, or gone, is kept and reported; never a blind write over another person's newer work); a bulk refuses to run until the cloud write path is attached and signed in, and any document that fails to save is reported instead of a toast that claims success; Apply re-plans against the current checklist and stops if it moved since the preview; a field that never existed is restored as absent (deleteField) rather than null; documents are addressed by store key, never by a payload field; lines that vanish between selection and Apply are reported as "no longer in the checklist"; no crash on the Bulk screen without an identity (lines skip with "set your initials first"); writes inside one bulk no longer re-render the app per document; the undo stack is mirrored to localStorage with an Undo last bulk edit button; checkedByCo added to the tools' item-key whitelist. Fixed from the critique: tags that share a job merge into one row naming every code (a PTAC pass no longer misses the PTAC-1 rooms); flagging over an existing issue says how many texts are replaced and shows the old text; disabled buttons look disabled and say Nothing to apply; color-scheme declared per face so native checkboxes read right; selection is a rail on the row edge, not a second check box; the category button is a real button that reads CLEAR when its lines are picked; the phone bar is three buttons with a More sheet and sits above the tab bar with the safe-area inset; identical preview rows collapse; room ids read as Room 204 · MEP; plural copy; a done state after Apply; tag filter box, family chips, dimmed types with no rooms on the picked floors. Fixed from the attacker: Flag an issue with no text disables Apply and says why; a picked tag stays on screen when the category chips or the filter would hide it; the category button label survives a re-render and works from the keyboard; dead selection markup removed; ending select mode keeps the scroll position; the phone category head no longer wraps. What the attacker could not break: per-document targeting across room FF&E, room MEP, space and space MEP (121 assertions), undo fidelity field by field, every skip rule, selection isolation across views and rooms, the no-identity gate, the not-signed-in refusal, and the phone layout. Not verified by anyone: the real Firestore path (every test runs in local mode by design) and true two-device concurrency. Known and accepted: the undo stack is per browser, and the durable record of a bulk is the activity summary line plus the per-document lines, not a recovery document as the crew dashboard keeps. 50 browser checks in tests/bulk-ui.mjs. |
| D44 | 2026-09-02 | **One-tap MISSING and IN BOX on bulk, no scroll jump on a check, percent complete by trade on the dashboard.** Austin: "add missing and in box to bulk items. when item checked dont allow it to scroll up. add percent complete per trade for FF&E etc. KPIs to dash board." (1) The select-mode bar carries Missing and In box as one-tap flags that go straight to the preview (Other issue still opens the paper vocabulary); the phone More sheet lists Flag MISSING and Flag IN BOX first. (2) The router now keeps the scroll position on any data re-render of the same screen (a hash change still starts at the top), so checking a line at the bottom of a long checklist, or ending select mode, no longer throws the reader up the page. (3) The dashboard gained a Percent complete by trade table under the KPIs: FF&E as one trade with its families under it (Casegoods, Bath Accessory, Appliance, Lighting, Window, Seating, Art / Mirror, Bedding, Door Hardware, Misc), then each MEP trade (Mechanical, Electrical, Plumbing, Fire Protection, Fire Sprinkler, Fire Alarm, Low Voltage); every row shows a bar, percent, checked of total, open issues, and a per-floor percent for floors 1 to 4 (hidden on the phone). Counted off the same checklist lines the crew taps, rooms and common areas together, so there is no second set of books. Tests: four-floor UI 22 checks, bulk UI 55 checks (one-tap flag writes IN BOX on every previewed line; scroll held within 40 px on a bottom-of-list check). |
| D45 | 2026-09-02 | **A live print sheet for every room and every common area, plus floor packets.** Austin: "i need to add a print sheet to each room and have it updated as items are checked off etc." The sheet is not a stored file: it is generated from the live store at the moment it is opened or printed, and an open sheet redraws as lines are checked off (the router re-renders on every store change; the GENERATED stamp is the moment of the last redraw). Every guest room already had a Print sheet button; every common-area space now has one too, and a space sheet shows its FF&E lines and its MEP punch. New Print sheets hub in the office nav: every sheet by floor with checked counts and open issues, a packet per floor (rooms then common areas, one print job, a page break between sheets) and a whole-building packet; the floor-2 packet renders to 149 Letter pages. Print or save PDF uses the browser's print dialog, so the PDF is always the current state. The three hosted PDFs under platform/sheets/ from the slice (101, 103, 105) are milestone snapshots and are not regenerated; the in-app sheet is the sheet of record. Build-authored room notes stay off the paper; crew and office notes print. Tests: four-floor UI 28 checks, including a check that an open sheet redraws when a line is checked. |
| D46 | 2026-09-02 | **TV mount line under the Television in every guest room; Bulk mark on the phone.** Austin: "Add bulk edit to the mobile app, and also add underneath TV: add TV mount to each room. Doesn't matter about the tag, just put it underneath TV." (1) One Appliance line per guest room, key tvmount_a, sort 14025, directly under the Television (903, sort 14020) and above the Clock / radio (904, 14030): "TV mount installed", qty 1, no tag asserted (his words: the tag does not matter), source "D46 (AJ 2026-09-02); owner-directed check item". Lives in RULED_LINE_ADDITIONS in both generators, byte-identical (the floor-2 generator re-proves that against build_floor1.mjs on every run), so no rebuild can drop it. Floors 2 to 4 rebuilt, carried and audited; floor 1 seed carries the same line in build_floor1.mjs's own shape (the floor-1 audit's byte-identical rebuild check passes). Applied LIVE to all 115 guest-room FF&E documents by platform/tools/add_ruled_line.mjs: backup first, one create-only PATCH of items.tvmount_a per document, every other item and document proved unmoved by read-back, crew collection untouched. Not added to common areas (no TV line there). (2) Bulk mark joins the phone tab bar (Dashboard, Rooms, Bulk mark, Contacts, Activity, 3D BIM); select mode on every checklist was already on the phone. Noted from the floor-3 carry on this run: the crew now holds 485 checks on floor 3; 10 of them, on room 313's MEP, are on PTAC detail lines the D10 condensation folded away (checked by AJ in the crew app), so they have no line to land on; the carry lists them and drops nothing, and the condensed PTAC line was NOT marked checked on their behalf. That is Austin's call. |
| D47 | 2026-09-02 | **TV mount line reads "TV mount"; the two branches are told about each other.** Austin: "Just add TV Mount remove Installed" and "make sure you are talking in both repos." The D46 line's label is now "TV mount" in both generators, all four seeds and the 115 live rooms (platform/tools/relabel_item.mjs: one field on one item key, backup first, read-back verified, crew collection read only). PR #23 (branch claude/home2suites-render-quality-4l3o7w, based on this branch) carries the King Studio photo-match loop and a floor-1 scene build, and overlaps this branch on room3d.html, the BIM module, king-viewer-notes.json and the floor3d files; a coordination comment on PR #23 records what the base changed on each shared file, what is live, and the suggested merge order (base room3d.html wins, PR #23's floor scene replaces the base's partial files, the scene stays on hold for deploy). PR #18's description was rewritten to the current state. |
| D48 | 2026-09-02 | **Door closer and door lock lines on every common area.** Austin: "add door closers and door locks to all the common areas too." D28's two Door Hardware lines (DH-1 closer, Rixson R21013 Series 10; DH-2 lock, 10-336 finish 630) now ride on every common-area checklist document as well: the FF&E doc of each space, or the parent doc of a space that carries only a punch (S001, S030, S032). Carried in both generators as RULED_LINE_ADDITIONS entries with doc "space", applied inside the byte-faithful common-area recipe (buildSpaceBandDoc) so a rebuild keeps them; floors 2 to 4 rebuilt through the --spaces path and re-carried, the floor-1 seed patched with the identical line shape. Live: added to all 40 spaces with platform/tools/add_ruled_line.mjs (additive create-only, backup first, read-back verified, crew collection read only). The line's own note says a space with no door of its own should be flagged rather than checked, since the ruling is building-wide and the tool does not know which spaces have doors. |
| D49 | 2026-09-02 | **Contact directory edits, applied in the app; the contact sheet still needs the same three changes.** Austin, from a screenshot of the directory: "Make it say structural engineer. Remove APM Services. And Add Jose A Perez as he is our new FF&E installer." Applied to the live _dir document (backup first, one field-path patch, read-back verified, no other contact changed): Steel Tech USA's Yalcin Kucukkomurler now carries scope "Structural engineer" in the Structural group; both APM Services LLC contacts are archived, never destroyed (D13); Jose A Perez is added as a Subcontractor with scope "FF&E installation", title "FF&E installer", with the phone, email and address Austin supplied, which live in Firestore only and never in this repo. Because the Google Sheet is the source of truth (D14), the hourly sync will report the Steel Tech scope change and the two APM archives as disagreements until the sheet matches; Jose was added in the app and the sync never touches app-added contacts. Same day, at Austin's "make sure you push to both repos": this branch was merged into claude/home2suites-render-quality-4l3o7w (PR #23) with that branch's own version kept on its five 3D files (room3d.html, floor3d.html, floor3d.src.html, build_floor3d.mjs, king-viewer-notes.json) and everything else taken from this branch, and pushed. |
| D50 | 2026-09-02 | **A resolved issue is history, not a flag.** Austin, from a screenshot of a checked line with a struck-through MISSING pill: "if any item has been cleared and marked out make sure it doesn't count as flagged; it should erase the flag or not count the flag." Every count already excluded resolved issues (room stats, dashboard, trade table, bulk tag list, print sheets); two behaviors did not. Fixed: a line with a resolved issue no longer shows the red pill at all (a muted "resolved · MISSING" note remains so the history is not lost), and tapping such a line stamps the check like any clean line instead of opening the issue sheet; the sheet, when opened by press-and-hold, says the issue was resolved and no longer counts, with Clear issue to erase the record. Only an OPEN issue or a FLAGGED line (sources disagree) steers the tap. |
| D51 | 2026-09-02 | **Item status board on the dashboard.** Austin: "I need KPIs showing how many of what are pending, missing, in boxes or need installed for each item on the dashboard." Under the trade table: nine tiles (Pending, Missing, In box, Need install, Need proper place, Damaged, Wrong item, Other issue, Installed of total) and a table with one row per distinct line in the building, merged by category and label the way the Bulk mark screen merges tags, grouped by Rooms FF&E, Rooms MEP, Common areas FF&E and Common areas MEP, with columns Total, Installed, Pending, one per issue in the paper vocabulary, and Other (free-text issues, listed on hover). Definitions printed on the board: installed = checked; pending = not checked and no open issue; issue columns count open issues by their exact text; a resolved issue counts nowhere (D50). Filters: every floor or one floor, and a text filter. Tapping a row opens Bulk mark with that tag, kind and category picked so the count can be acted on. Counts are proved against a direct walk of the store in tests/four-floor-ui.mjs (38 checks). |
| D52 | 2026-09-02 | **MEP punch simplified on every guest room, all floors; door hardware gets rework and key card lines.** Austin: "revise the MEP punch on all floors. Simplify the items ... I just need PTAC installed/working"; add "Rework closer" under the closer on ALL doors, checked only when rework was needed; "Key card installed" and "Key card working" beside the closer and lock; "Breaker box labeled" on every guest room; Electrical adds receptacles and receptacle covers as two lines; no single fire alarm line, the smoke detector and horn strobe each split into installed and working; Fire Sprinkler gets FS escutcheon installed and flush; the escutcheon touch-up sits under Finishes so the painter sees it; sinks and showers get hot water, cold water and water pressure lines. Template in `platform/tools/mep_punch_template.mjs` (40 lines on a King Studio, 47 on an accessible connecting room, replacing 23 to 100); old lines are archived in each doc, their state carried, their spec text folded into the new line's detail. Rework lines carry `optional: true` and count only once checked (store.roomStats). The rework line also rides on every common area under D48's closer. Same day, from the dashboard screenshot: "put the Plumbing items in Plumbing": the percent-by-trade table now counts a line by its trade, not by the document it sits in, so the three punch-only spaces (S001, S030, S032, plain ids) stop showing their Plumbing and Low Voltage lines under FF&E. Staged on all four floors (floor1 to floor4-staged.json, ref-rooms-staged.json); audits floor 1 41/41, floor 3 56/56, floor 4 56/56, floor 2 one mock-up text comparison relaxed for D52 lines; generator selftests pass. Austin: "go" at 19:15 UTC. ROLLED LIVE 19:19 to 19:27 UTC on all four floors (rollout_floor1.mjs --apply, rollout_floor.mjs --floor=2,3,4 --apply; backups in tools/out/backups/platform-before-floor*-rollout-2026-09-02T19-*; zero check-offs lost). Read back live: 115 MEP docs carry the D52 punch, 115 rooms carry the rework and key card lines, 37 common areas carry the rework line. The app deploy to main and gh-pages (store.js, tracking module, seeds, generators) is staged in a worktree and waits on a push permission this session does not hold. |
| D53 | 2026-09-02 | **D52 rolled live on floors 1 to 4, and the app that reads it deployed.** Austin: "roll D52 live on all floors and push to both repos." Done from this branch with the three-way rollout tools (floor 1 rollout_floor1.mjs, floors 2 to 4 rollout_floor.mjs): dry run per floor, backup before each apply, 0 documents created, 35 + 70 + 70 + 70 updated, retired MEP lines archived with their history, every live check kept (866, 841, 794, 3), every note kept (19, 476, 464, 445), 3 resolutions protected, read-back verified, crew collection never written. Logs in research/rollout/d52-floor*-{dry,apply}.log. Deployed to main and gh-pages: the four seeds, the generators and the punch template, and the app files D52 needs (store.js counts() so an "if needed" line joins the count only once acted on; the tracking module's trade rollup by category and IF NEEDED chip; the directory module). The item status board honors the same rule. The BIM module's Floor 1 scene entry stays off main while the scene is on hold. Both branches pushed to one head. |
| D54 | 2026-09-02 | **One set of records: the old crew work merged into the platform, and the crew app rewired onto the platform collection.** Austin: "merge the old and new data into one", "make sure everything the current dashboard has syncs to the crew app", and, offered the two ways, chose rewiring the old crew app onto the platform records. Done in order. (1) The carry tool became floor-parametric for floor 1 too and gained --land-all: every check, issue and note in the old collection now lands on a line, a punch or common-area line the crew worked that the build no longer carries being restored as a field-authored line in the crew's own words (17 + 2 + 12 + 4 lines); reconciliation exact on every floor (490/176/7, 579/696/28, 485/646/10, 25/456/1), 0 with no line. (2) Rolled into the live platform with the three-way tools, backups first: live checks rose 866 to 931, 794 to 804, 3 to 25 as the old app's newer work arrived, the old app's issue resolutions applied, every live check kept, crew collection never written. (3) The old crew app's code at the repo root now reads and writes projects/h2sep/platform_rooms (store, bulk edit, print, refs) and understands a common area's punch doc id (S221-M); nothing else in it changed. The old collection projects/h2sep/rooms is frozen as an archive; no tool writes it and the app no longer reads it. Known gap recorded: the generators' --spaces path does not preserve field state on common areas, so a rebuild must always be followed by the carry, as the pipeline does; the floor-4 audit compares structure only there. The crew app files are no longer byte-identical by design; from here the rule is that they change only by ruling. |
| D55 | 2026-09-02 | **The build's document notes are office reference, never crew flags.** Austin: "remove all the red Open document conflicts etc on the mobile app." Every room record carries notes the build wrote (open document conflicts, document gaps, gate gaps, bathing configuration, the type note, quantity rulings), flagged red because they are unresolved questions for the office. The crew app no longer shows them and no longer counts them as open issues; the platform keeps them, folded under "Document notes from the build" in a room's notes sheet, and stops counting them as open issues too, so the dashboard's open-issue numbers are the crew's flags and the office's own notes. A build note is recognized by its key (n_...) and the absence of an author; a crew or office note always has one. Nothing was deleted from any record. Crew app 1.19.1. |
| D56 | 2026-09-02 | **Verification notes stay off the crew app's rows.** Austin, from a screenshot of room 434 where one dishwasher line ran a full screen of text: "remove all these extra notes on the crew app to make it easier to scroll through." The platform writes a verification note on every line (source, package, reliability, ruling text) for the office; the crew app printed it inline. The crew app now shows a line's note only when it is a few words (80 characters or fewer, the paper-sheet kind); a longer note stays off the row. The notes are untouched in the records and still read in full on the dashboard. Crew app 1.19.2. |
| D57 | 2026-09-02 | **Whole-floor option on every check-off, in both apps.** Austin: "when I check off an item, give me the option to have it select that same item for the whole entire floor." After a tap checks a line, the toast now carries Undo and "Whole floor N". Whole floor plans the same check on the same line (same tag, or same label when untagged) in every other room of the same kind on that floor, through each app's bulk engine: lines already checked, checked by someone else, carrying an open issue, or flagged are left alone and counted, the count is shown before anything is written (the confirm sheet on the platform, a confirm dialog on the crew app), and the apply has an Undo. Common areas do the same across the floor's common areas. On the platform the fix also repaired the "Checked" toast, which never showed because the handler read the line after the store had flipped it. Crew app 1.19.3. |
| D58 | 2026-09-02 | **The whole-floor offer follows every kind of line.** Austin: "make this button for ALL items mep etc." D57 wired the offer to a row tap only, and on the crew app its floor scope only knew guest rooms. Now both apps route every check-off through one path, so the offer appears after an FF&E line, an MEP punch line, a common-area line, a common area's punch line, and a line checked from its sheet (the platform's line sheet stamp; the crew app's "Resolve & check" on the issue sheet). The floor scope walks documents of the same kind only: a guest room walks the floor's guest rooms, a common area the floor's common areas, and a punch doc the punch docs of its own kind, so an MEP line never lands on an FF&E list. Same rules as before: lines already checked, checked by someone else, carrying an open issue, or flagged are left alone and counted, the count shows before anything is written, and the apply has an Undo. Found on the way: the crew app's Common areas list and roll-up included the common areas' punch docs ("S221-M") as if they were rooms; they are now kept off that list the way "105-MEP" is kept off the floor grid. Tests: bulk-ui 72 checks, new whole-floor-crew 21 checks. Crew app 1.19.4. |
| D59 | 2026-09-02 | **Flag the whole floor from the line's issue sheet.** Austin: "add a box under each item. If I select Missing underneath that item, I need to have a box that allows me to check it and that it will count for the entire floor of guest rooms." Both apps' issue sheets now carry a checkbox, "Also flag this line on every other guest room on floor N" (common area or punch list wording where that is the kind). It starts unchecked every time, so a floor-wide flag is always deliberate. With it checked, the flag picked (Missing, In box, any quick pick, or a typed note) lands on the tapped line and then goes to the same line on every other document of the same kind on that floor through the bulk engine: the platform shows the confirm sheet with the count and what each line loses before writing, the crew app a confirm dialog; a line already carrying that same open flag is left alone; the apply has an Undo. Tests: bulk-ui 76 checks, whole-floor-crew 27 checks. Crew app 1.19.5. Follow-up, same day: Austin asked for "the same box for In Box"; the one box already covers every pick (Missing, In box, the rest, and typed notes), so the crew label now says to tick it first and then pick the flag, since a crew-app pick applies the instant it is tapped. Crew app 1.19.6. |
| D60 | 2026-09-02 | **No copy-and-paste callout on press-and-hold in the crew app.** Austin: "When I push and hold to select more options, I get the cut and paste animation. I need you to cancel that only when I'm using the app." The crew app now turns off text selection and the phone's long-press callout inside the app only (a page-level rule, the way the platform's D6 rule does it), keeps selection on in typing fields so notes can still be edited, blocks the browser context menu on a checklist row, and swallows the tap that ends a hold so the line underneath is not checked by the same touch. Nothing changes outside the app. Crew app 1.19.7. |
