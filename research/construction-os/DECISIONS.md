# Platform build decisions (Austin's rulings)

Running log. Each entry is law until Austin overrides it in writing.

| # | Date | Decision |
|---|---|---|
| D1 | 2026-08-15 | Status workflow v1: tap-to-check with initials + timestamp stays the heartbeat on every item, plus the issue flag. Procurement statuses (Not Ordered, Ordered, In Transit, Received, Staged, Installing, Installed Complete, Damaged) turn on only for order-tracked categories (FF&E families, appliances, bath accessories, patio, pool). Other categories can enable them later via a per-category switch, no rebuild. |
| D2 | 2026-08-15 | Common areas: the 66 spaces seeded from the plans carry over as the confirmed list. Additions/archives happen in-app later. |
| D3 | 2026-08-15 | Populating categories beyond the database scope (cameras/low voltage, pool equipment, patio furniture, kitchen equipment, signage): both paths. Mine plans + submittals in Drive where they cover it (sheet-cited, flagged unknowns, never guessed) AND ship a fast in-app add-item intake flow for field fill-in. The database stays the source of truth where it has the data. |
| D4 | 2026-08-15 | Design direction: **Command Deck** (of three pitched: Command Deck, Sheet Set, Night Shift). Print-calm office command center (dark sidebar, white field, big tabular numerals, title-block accents) + dark glare-proof crew phone app. Cyan #02A9DE rationed to decisions: active nav, primary actions, spec links. This is the reference look for every screen mock and every built screen. |
| D5 | 2026-08-17 | Mock set approved as reference. Build starts with a vertical slice: **3 guest rooms on floor 1 at 100%** — rooms 101, 103, 105 (all carry their own correct 3D geometry; 101/103 connecting pair). Full real checklists for each room (FF&E and MEP punch) with live field state, initials redacted to initials-only in anything committed. |
| D6 | 2026-08-17 | Press-and-hold must NEVER pop text selection (bug in the first build). All interactive checklist surfaces ship with user-select none and touch-callout none; long-press is a deliberate gesture. |
| D7 | 2026-08-17 | **3D BIM gets its own entry in the left section menu.** Every room shows its CORRECT per-room geometry exactly like the first build (A555 dims, mirroring by reflection, connecting-door logic). Rooms without their own model (King family) keep the honest hard-stop, never a relabeled shell. |
| D8 | 2026-08-17 | Firebase signup deferred until Austin reviews the cost math (brief delivered). Slice builds on a local store shaped like the Firestore API so the backend drops in without rework. |
| D9 | 2026-08-18 | Cloud setup done, in the EXISTING Firebase project (H2SEP Checklist, `h2sep-checklist`), which Austin's account already owns: Blaze plan active, $25 budget alert set, Email/Password auth enabled (Anonymous kept for the crew app), Storage bucket created. Platform data lives in its own fenced collection `projects/h2sep/platform_rooms` (rules published by Austin 2026-08-18; deletes blocked; crew `rooms` untouched, verified by updateTime). Slice seeded and verified: 101/103/105 + MEP docs, 308 items total. Rollout stays HELD pending Austin's slice verdict. |
| D10 | 2026-08-18 | **Guestroom MEP punch condensed per Austin, flags closed by his ruling.** Mechanical = 4 lines (PTAC, thermostat, room grille, bath exhaust grille; his exact spec). Plumbing = his keep/remove markup: kept WC, lavatory faucet, shower, shower head, shower enclosure, floor drain, trap guard, kitchenette sink; removed supply stop, lav bowl, reject-check line, anti-scald valve, gate valves, PTAC condensate. Fire Protection = sprinkler heads + smoke detector (horn line removed by Austin). Electrical 5 and Low Voltage 3 are condensed drafts awaiting his marks. Every folded detail and citation kept verbatim in the item sheet; condensed lines assert nothing any open document conflict disputes. Old lines soft-deleted, never destroyed. Applies to the platform slice; crew app untouched; document conflicts remain tracked in NEEDS-CLARIFICATION. |
| D11 | 2026-08-18 | **Submittals wired into the slice, flags driven to one open question.** Per-room Drive print sheets linked in-app (10 rooms have sheets; Room 103 has none in Drive, flagged to Austin). Submittals attached to items: Danby DDW2404EBSS dishwasher (closes the 18-vs-24-inch flag; submittal governs), Moen MGXP33C disposer (closes the existence flag), Danby fridge, Danby OTR microwave, WINGITS WRD5SS36SN divider hardware, Moen vanity + kitchenette faucets and American Standard vanity + kitchenette sinks (submittal governs over plan-scheduled Delta). GR-308 working-wall naming flag closed as naming-only per Austin's remove-all-flags directive. Remaining open: GR-322 nightstand count (1 vs paper sheet's 3), question to Austin. Rollout to all rooms stays HELD until Austin approves. |
| D12 | 2026-08-18 | **Nightstand ruling: rooms with two queen beds have THREE nightstands (right, middle, left).** The room 101 paper sheet's GR-319/GR-322/GR-323 count was correct; drawings tag the family once. GR-322 line now qty 3, flag closed. **Slice is at ZERO flags.** Print sheets: the printer icon opens a live in-app sheet generated from current data (like the first build's print.html); rev 08-18 PDFs for 101/103/105 hosted under /platform/sheets/. Sheets for the other 112 rooms generate at rollout after Austin's approval. |
| D13 | 2026-08-18 | **Contacts and Assignments are live (module three: `directory`).** The real project contact list (47 people, 30 companies, from H2SEP_Contact-List_2026-08-14) is in the app: grouped by category, searchable, one tap to call or email from the field. Assignments put a company on a scope (a category) across a room set, with a role (Install or Supply), a due date, and a note; progress is counted live off the same checklist lines the crew taps, so there is no second set of books. Six assignments were auto-matched from the contact list's own Scope column (Mechanical, Electrical, Plumbing, Fire Protection, Low Voltage, Appliance) and are labeled AUTO-MATCHED · CONFIRM until Austin confirms them; nothing was guessed, so the FF&E scopes sit in the Not assigned yet list. Every room's checklist now shows who owns each scope, and tapping that chip assigns it. Data lives in two docs (`_dir`, `_asg`) shaped exactly like a room doc, so the rules Austin already published cover them with NO rules change. **The contact list carries personal cell numbers and emails, so it lives in Firestore only and never in the public repo.** Records archive, never delete. Zero data loss verified: all six room docs unchanged (same updateTime, same item counts) before and after. |
| D14 | 2026-08-19 | **The Google Sheet is the source of truth for contacts, and it syncs itself.** `tools/appsscript/SyncContacts.gs` lives inside the contact list spreadsheet: hourly plus an on-demand menu item, it pushes the sheet into `_dir` and every open app picks it up in seconds. It writes only changed fields, archives rows deleted from the sheet instead of destroying them, never touches contacts added inside the app, and **never overwrites a contact edited in the app since the last sync** (it reports the disagreement so Austin picks the winner). Sign-in is the same anonymous sign-in the crew app uses, so there is no service account, no key file, and nothing to install on his work computer. `tools/sync_directory.mjs` is the same logic on the command line for audits and restores. First sync applied Austin's two additions (Hilton, Vibrant Services) and his removal of Edward De La Garza's phone number. His in-app edit of the Texas Fire scope to "Fire Protection/Fire Sprinkler" was held, because the sheet still reads "Fire protection". |
| D15 | 2026-08-19 | **A check now records the company, not just the initials.** The identity sheet asks who you work for, offering every company in the directory (and matching it automatically when your name is already in the project directory). The company travels inside the same atomic check group as the initials and the timestamp, so the record says who signed the line and for which outfit. It shows on the checklist line, in the item sheet, in Activity, and as a SIGNED BY legend on the printed sheet. Checks carried over from paper keep their initials and simply show no company, never a guessed one. |
| D16 | 2026-08-19 | **Incident, fixed: a merge write wiped 46 contacts in the cloud.** Saving a contact edit in the app called `createIfMissing`, which used `setDoc(..., {merge: true})` with an empty `items: {}` in the skeleton. An empty map is a leaf in Firestore's merge field mask, so it wrote the empty map OVER the live one, and the only record left was the one being saved. Found within the hour by the sync tool's diff. Fixed at the cause: `createIfMissing` now reads first and writes only into a genuinely empty slot, and `ensureDoc` returns early whenever a copy already exists. Both guards are covered by a regression test. Restored from the sheet, 49 contacts verified, Austin's in-app edit preserved. No room, punch, or assignment data was touched; every room doc kept its exact updateTime through the whole episode. Standing lesson: never merge a skeleton over a document that can already hold records. |
| D17 | 2026-08-20 | **The blind critic judges the 3D render against real photographs of the demo room.** No perspective view or rendering exists anywhere in the project: A550, A551, A552, A555 and all 41 RK Hospitality ID sheets are orthographic plans, RCPs, finish plans, electrical plans and flat wall elevations, confirmed by OCR of the sheet images and the ID-0.0 drawing index. The only real-world images are the 25 photos in Drive folder '8. Photos of Demo Room' (2026-08-12), and the Drive connector returns no bytes for JPEGs, so Austin is sending them directly. Until they arrive the 3D work is checked against the A550 and A555 interior elevations at a matched orthographic camera, which tests correctness (right item, right count, right place, right size) rather than photorealism. |
| D18 | 2026-08-20 | **Floor-1 common areas use the PLAN numbering** from the architectural drawings (the reference database), not the live QC Deficiency Tracker numbering. The two disagree on 22 of 28 spaces (Lobby 003 not 001, Reception 004 not 002, Sales 022 not 003, Fitness 023 not 005, and so on). The drawings govern. The tracker's numbers are a field artifact; reconciling the crew's paperwork to the plan numbers is a separate cutover task, not a reason to seed the wrong number. |
| D19 | 2026-08-20 | **Room 118 (King Studio Acc.) gets the ROLL-IN SHOWER package, not a tub.** A554 and A103 draw a roll-in, and the FF&E Installation 1st Floor tab carries 16 shower bases (6034-L) and 16 shower doors (NOVA-6076) with no tub line for 16 rooms. A533's 'accessible tub' text is superseded. The eleven mutually exclusive tub rows in the database (BT-1 bathtub, 2 inch tub waste, HD-05 bowed rod) are dropped for 118; the roll-in rows (SH-1/SH-3 pan, kn 10 diverter) govern. Room 118 is unblocked. |
| D20 | 2026-08-20 | **GR-202 Nightstand Sconce is 2 per King room, 16 on floor 1.** The FF&E Installation 1st Floor tab reads 33, which is the value the 2nd, 3rd and 4th floor tabs carry and is impossible on a 16 key floor where every comparable line reads 15 or 16. The database gives the King Studio two nightstand sconces per room across the 8 King family rooms, which lands on 16. The sheet's 33 is a copy-down error. |
| D21 | 2026-08-20 | **The demo room is ROOM 110, a King Studio, and its 24 photographs are the comparison target.** Austin confirmed the number; two photographs independently show '110' hand written in pink marker on the corridor wallpaper beside the door. The photographs were delivered in chat, not through Drive (the Drive connector returns no bytes for JPEGs), and were recovered at full 2000x1500 resolution from the session transcript to /tmp/demo-room, so that fresh-context critic agents can open them. **They stay out of this public repo**: they are photographs of a client's unopened hotel. The written record of what they show is at research/king-studio/demo-room-observations.md. Room 110 is one of the six plain King Studios (104, 106, 108, 110, 112, 114), so the geometry and package derived from these photographs apply to the whole King Studio type. |
| D22 | 2026-08-20 | **The plain Queen-Queen working wall is GR-305, not GR-308. This supersedes D11 on that line.** Austin: "fix GR-305 on 105-115 and regenerate 105". The FF&E Installation workbook's 1st Floor tab carries the two as separate purchased parts with separate counts: GR-305 Working Wall @ QQ at L 2 plus R 4 = 6 units, and GR-308 Working Wall @ QQ Connector at L 1 plus R 1 = 2 units. Floor 1 has exactly 6 plain Queen-Queen keys (105 107 109 111 113 115) and exactly 2 QQ connecting keys (101, 103). Six and two, no remainder. `data/project.sqlite` carries NO GR-305 row anywhere in the building: every QQ room was transcribed as GR-308, and the transcriber flagged the oddity in the row itself. D11 closed that flag as a naming quirk with no order impact. D11 was decided without the workbook and was wrong, because two different parts were actually purchased; a crew told to install GR-308 in room 109 would hang the wrong casework. **Still open, deliberately not guessed:** the workbook splits GR-305 into 2 LEFT and 4 RIGHT across those six rooms and no document states which room takes which hand, so the line ships as the base tag at reliability MEDIUM with the handedness called out. Confirm with RK Design before the casework is released. **Room 105 was regenerated** using a new `--regen` path that is create-only by default and cannot destroy: the old gr308_a line is kept as a tombstone with `deleted: true` rather than dropped, so any check-off already recorded against it survives; all 77 of 105-MEP's deleted history rows were carried over untouched; every other field that moved is reported line by line. Its sprinkler head count moved from 1 to 3 so it stops disagreeing with the five other rooms of its own type. Zero keys lost, and rooms 101 and 103 stayed byte-identical. The generator selftest was bypassing the correction, so it now walks the same path the build walks and expects exactly the ruled difference. **Migration note for cutover:** live room 105 still holds gr308_a. The rollout must soft-delete it and add gr305_a, never drop the line. |
| D23 | 2026-08-20 | **The crew's work is carried into the floor-1 build, and a rebuild can no longer erase it.** Austin: "make sure to carry all of the checked off items notes etc. to the this build". The live crew app holds real field work on floor 1: **382 check-offs, 289 open issues and 5 notes across 19 documents**, dating from July. The staged rooms were born clean, so rolling them out as built would have wiped months of field time. `platform/tools/carry_field_state.mjs` reads the crew collection (READ ONLY, snapshot taken first) and carries checked, initials, checkedAt, checkedAtLocal, issue and issueResolved onto the matching lines. Item keys matched 624 of 629 directly, because both builds share a generator lineage. The 5 that did not are the working wall retagged under D22: the crew checked that wall off as GR-308, so the work is carried across to GR-305 as a ruled remap, and room 105's retired line keeps its original check as history rather than counting twice. **Reconciliation is exact, not approximate: 382 to 382 checks and 289 to 289 issues on live lines**, and the run fails if it is not. **PII: 755 name and account-id fields were dropped on the way in.** The seed is a public repository, so `checkedByName` and `checkedByUid` never enter it and note authors are reduced to initials ("Morgan Davis" becomes "MD"). Initials and timestamps are what the paper sheet carries and what the approved slice already carried, so that is the line. **A separate defect found and fixed while doing this:** the generator reset every check to false on rebuild, so any later `--regen` would have silently zeroed the floor. It now re-applies field state from the staged copy onto freshly built lines and reports any work whose line no longer exists. Covered by a regression check. |
| D24 | 2026-08-21 | **FLOOR 1 IS LIVE. Austin: "go ... let's push this fucking thing live: live dashboard and live app."** The rollout ran per CUTOVER-FLOOR1.md: backup first, three-way merge, read-back verify. Result: 81 platform docs (plus _dir/_asg = 83), 1,554 item records, 382 check-offs and 288 open issues live, the one conflict (101/hd08_a, resolved in the platform app) kept in the live app's favour, and the crew collection untouched (updateTimes unchanged, verified). The deployed app now seeds from the full floor-1 build with the approved slice as fallback, the sidebar reads FLOOR 1 LIVE, and MODEL_ROOMS covers the whole QQ family (101/103/105/107/109/111/113/115) since A555 geometry exists for all of them; the King family keeps the D7 hard-stop. **STANDING DIRECTIVE FOR FLOORS 2, 3 AND 4, recorded as law:** when those floors are built, ALL notes, markups, check-offs, initials, timestamps and issues from the old crew app come with them, and NOTHING is deleted - the same carry_field_state discipline used on floor 1, with exact reconciliation and PII redaction. A floor build that arrives clean is wrong by definition. Also standing: several floor 2-4 room types (King One Bedroom, King One Bedroom Acc., QQ Extended, QQ Acc.) have no approved reference room, so one reference room per type gets built and approved before the type generates at scale. |
| D25 | 2026-08-21 | **The floor-1 3D assembly is NOT built. Stopped on a usage limit, not on a decision.** Austin asked to complete the King-family models and assemble all of floor 1 into one navigable scene. The workflow was authored and launched (run wf_a6b0cb81-2e3: A100 layout recon, King geometry spec, King rooms into platform/room3d.html, an assembled platform/floor3d.html with live status paint and click-through, a dimension verifier, and three design critics). **All eight agents failed on the Fable 5 usage limit before doing any work.** Nothing was written: no floor3d.html, no geometry-spec.json, working tree clean, room-3d.html byte-identical. The plan is intact in the workflow script at `.../workflows/scripts/h2sep-floor1-3d-wf_a6b0cb81-2e3.js` and can be re-run as-is on a model with available capacity. **What DOES ship in the 3D today:** the whole QQ family (101/103/105/107/109/111/113/115) renders its own correct A555 geometry in the live app; the King family keeps the honest D7 hard-stop rather than a relabeled QQ shell; and platform/king-studio.html exists as a separate materials-and-light exhibit of the King Studio, dimensionally built to A550. |
| D26 | 2026-08-24 | **Austin approved the Electrical and Low Voltage condensed punches as drafted.** Electrical stays 5 lines (panelboard, GFCI, lights, outlets, sink switches) and Low Voltage stays 3 (WAP, TV/data/desk jacks, phone/doorbell). "Electrical - looks good ... Low voltage is good." With plumbing (D10) and mechanical already his, every trade band of the guestroom MEP punch is now owner-approved. Also ruled in the same message: GR-305 handedness stays OPEN, he will find the answer himself ("Leave this item I will find out answer later"), and the room 118 tub-mark RFI is ON HOLD ("Hold off on drafting the RFI"). |
| D27 | 2026-08-24 | **Hot and cold water verification added to every guest room MEP punch.** Austin: "Make sure to add hot and cold water work." One plumbing line per room (`plmb_hotcold_a`, sort 3018): run hot and cold at the lavatory, shower and kitchenette sink, both temperatures arrive, hot on the LEFT. Lives in the generator as a RULED_LINE_ADDITION so no rebuild can drop it. Applied to the staged seed and to all 16 LIVE rooms (backup taken first, additive create-only patch, 16/16 verified by read-back, crew collection untouched). |
| D28 | 2026-08-24 | **Door Hardware category added to every guest room FF&E checklist: closer and lock.** Austin: "add items to FF&E - 1. Door Closer installed ... 2. Door Lock Installed", with site photos of the delivered product. Identities transcribed VERBATIM from his photo labels, not guessed: closer RIXSON R21013 Series 10, UL Classified MISCELLANEOUS FIRE DOOR ACCESSORIES 2MF0; lock box label NORTON RIXSON / ASSA ABLOY 10-336, DOOR, finish 630, one unit photographed installed on a guestroom frame. Two lines per room (`dh_closer_a`, `dh_lock_a`) in a new Door Hardware band at the end of the FF&E list. Generator-carried (RULED_LINE_ADDITION), staged and LIVE on all 16 rooms, verified. The photos themselves stay out of the public repo; the notes carry the transcription. **Standing sequence he set in the same message:** deliver the FULL mockups of the four unapproved room types (King One Bedroom, King One Bedroom Acc., QQ Extended, QQ Acc.) for his approval, then complete the per-room 3D BIM models and the per-floor BIM model. |
| D29 | 2026-08-24 | **Bed skirts on the ADA rooms.** Austin: "make sure to add Bed Skirts to the ADA rooms." The database already carries GR-603.1 King Bed Skirt on all five King-family accessible keys (118, 217, 317, 417, 438) and live room 118 has the line. The real gap his direction closes is the two QQ Acc. rooms, 238 and 338: each has two GR-602.ADA ACCESSIBLE open bed bases and NO skirt row, because the standard queens use a GR-600.1 Box Spring Cover that an open ADA base cannot take, and no queen bed skirt tag exists anywhere in the document set. Ruled line: Queen Bed Skirt, code BS-Q, qty 2, one per bed, matching the room's own two GR-602.ADA rows - a derived count, not a guess. No document tag exists, so the line says so and the size gets confirmed with the FF&E supplier before ordering. Carried as a RULED_LINE_ADDITION in the generator (applies only to accessible QQ Acc. rooms; floor 1 has none, so the live floor is unchanged) and queued for injection into mockup room 238 the moment the round-3 fix workflow releases the file. |
| D30 | 2026-08-24 | **The four room-type mockups are delivered for approval, with the critic trail printed on them.** King One Bedroom (202), King One Bedroom Acc. (217), QQ Extended (230), QQ Acc. (238): full FF&E and MEP packages, crew field state carried exactly (39/39 checks, 88 issues, 3 notes, PII stripped), D27/D28 lines present, and **D29's Queen Bed Skirt (bsq_a, BS-Q, qty 2) injected on 238**. Four rounds of build-verify ran with fresh adversarial critics each round; defect trajectory 21, 26, 25, 10. Round 4 changed strategy - room-wide negative claims went from 50 to 0 and every meta claim is now enforced by platform/tools/assert_ref_claims.mjs - and the count finally broke. **The 10 remaining findings are all prose self-consistency inside line notes; none is a data defect.** No round faulted a category, tag, quantity, mark, reliability value or the carried crew state in the final version. Loop stopped by the standing no-round-5 rule; the mock book shows all four verdicts and the trajectory on its front page, so approval is made with eyes open. One brief error caught by the round-4 fix agent and worth recording: room 202 is NOT an accessible key, so it correctly carries no GR-603.1 bed skirt - D29's reach is exactly the seven accessible keys. |
| D31 | 2026-09-02 | **The floor-1 3D scene was built and judged once, then destroyed by a container restart before its checkpoint commit; the rebuild is authored and ready.** Workflow run wf_a5dcbbb1-c5e completed all eight agents: the King family 104-118 built into platform/room3d.html with all eight rooms proven (118 measured off A552, pulled from Drive), and a new platform/floor3d.html assembling all 46 floor-1 spaces with live status paint, hover cards, click-through, and Overview / Guest Wing / Common Areas / Exploded views. Judged honestly before the loss: dimension verifier FAIL on five defects (an unproven 4'-3" shift encoded as a dead constant against the spec's own do-not-encode note; a false proof narrative on room 118's partition datum while the rendered geometry was right; three non-coincident GR-3 openings at 116/118 where the QQ pair already uses one datum; the packaged artifact never inlined the floor scene so its Floor 1 screen was a silent blank; the second accessible clear-floor circle in 118 drawn differently in the two viewers with no printed size or position). All three design critics STRONG, not WOWED: two named the 30 empty common areas, one named the open-issue marker that reads as a rug instead of a red pin at the door. The container restarted in the seconds between reading that result and committing; every generated file and every agent transcript was lost. What survived is committed: research/king-studio/geometry-spec.json, research/floor1-3d/a100-layout.json (A100 measured at a proven 6.75 pt/ft with an honest gap list: no pool deck on A100, the front of house is one open volume, several back-of-house rooms have label anchors only, revision 5 clouds on both connecting pairs), research/floor1-3d/king-spec-recon.json, and platform/room3d.html force-added past its gitignore so the King build can never be lost the same way again. The rebuild workflow (research/floor1-3d/rebuild-workflow.js) bakes every defect and critic fix into the build prompts and makes the build agents commit and push their own files at every milestone. It was launched and then stopped within a minute on Austin's "ok stop give me a hand off file for a new session"; nothing was written by it. Handoff: research/construction-os/HANDOFF-2026-09-02.md. D25 stands as history: this row supersedes its "NOT built" status. |
| D32 | 2026-09-02 | **FLOOR 2 IS BUILT OUT AND STAGED FOR APPROVAL - FF&E and MEP, not the 3D. Not live.** Austin: "start here at this repo but I need the 2 floor built out. Just the FF&E & MEP not the 3d bim yet." Every floor-2 key in data/project.sqlite rooms - 33 guest rooms, 66 documents - plus the three floor-2 common areas that carry a gated line (corridor 221 with an MEP companion, elevator lobby 237, ice machine 239; the other six spaces hold only finish rows the approved gate keeps out and get no document, listed as such). Generator platform/tools/build_floor2.mjs is build_ref_rooms.mjs generalised to a floor: room table read from the database, donor chosen by type (King Studio from 104, Queen-Queen from 105, QQ Connecting from 103, the four mock-up types from the donors the mock-ups used), the reduction recipe still copied byte for byte from build_floor1.mjs and re-proved on every run, sort convention measured per donor, sprinkler lines counting a room's own head rows and dropping the first-floor total, a rebuild preserving field state and carrying crew-only lines forward. The four mock-up rooms rebuild identical to the D30 mock-ups on shape and text. **Ruling D22 applied to the eight plain Queen-Queen keys on floor 2 and to no other type,** on the same evidence standard it used on floor 1: the FF&E Installation workbook's 2nd Floor tab lists the working walls as separate purchased parts and every count reconciles against the floor's key mix with no remainder (GR-304 17 = 17 King Studios; GR-305 11 = 8 plain Queen-Queen + 1 QQ Wide + 2 QQ Extended; GR-308 2 = the 2 connecting keys; GR-309R 1 = the 1 QQ Acc.; GR-315 and GR-316 1 each = 202 and 217, which the database already tags). The eight plain rooms ship GR-305 at MEDIUM with the hand open (the tab splits them 5 LEFT, 6 RIGHT; D26 records Austin is answering the hand himself). **Deliberately NOT retagged, evidence written on the line and in note n_d22, OPEN for Austin:** 201, 230 and 232 (the tab's eleven GR-305 walls only reconcile with them counted in, but D22 names the plain type only) and 238 (GR-309R purchased, spec and ID-5.9 say GR-309, A556 tags GR-308). **The crew's work is carried exactly (D24):** 579 check-offs, 696 open issues and 28 notes across 69 crew documents, identity reconciled by platform/tools/carry_floor2.mjs, 1,186 name and account-id fields dropped, note authors reduced to initials, crew collection proven untouched by updateTime. Two crew lines the build could not produce were carried rather than lost: room 202's GR-905 (FF&E - Misc, outside the gate, rebuilt from the room's own row as on the mock-up) and room 201's field-authored line "Lamp shades are in the room in a box." (no category, no citation; carried verbatim and labelled FIELD-AUTHORED). Audit tests/floor2-audit.mjs: 51 checks, all pass, including that a rebuild keeps every check and the space docs, that two independent builds are byte-identical, and that floor1-staged.json, slice-f1.json and ref-rooms-staged.json were never written. **Where floor 2 is stricter than floor 1:** 446 lines on same-type rooms are MEDIUM or FLAGGED where the floor-1 donor line is HIGH, every one from an OPEN conflicts-table entry carried onto the line (B4.5, B4.2, B3.1, A11, B4.4) or the worst-of-own-rows MEP rule the mock-ups already follow; floor 1 was built before that rule. Listed in the review book so the difference is a decision, not a surprise. Review book: research/floor2/floor2book.html. Rollout of floor 2 waits on Austin's approval and follows the floor-1 runbook (backup, three-way merge, read-back verify, crew collection never written). |
| D33 | 2026-09-02 | **Working walls on the floor-2 two-queen keys: 201, 230 and 232 take GR-305; 238 takes GR-309.** Austin, reviewing the floor-2 book: "ok retag 201, 230, 232 to GR-305 and 238 to GR-309". Closes the two working-wall questions D32 left open. Evidence on the line, verbatim from the FF&E Installation workbook's 2nd Floor tab: GR-305 Working Wall @ QQ at L 5 + R 6 = 11 units, which reconciles only as the 8 plain Queen-Queen keys plus QQ Wide 201 plus QQ Extended 230 and 232; GR-309R Working Wall @ QQ Accessible at 1 unit against the one QQ Acc. key, with the FF&E spec and ID-5.9 also naming GR-309 while A556 tags GR-308. Carried as TAG_CORRECTIONS_F2 entries in build_floor2.mjs (same mechanism as D22), so no rebuild can drop it; the corrected lines ship at MEDIUM with the hand open (the tab prints GR-309R; whether that R is a hand is not asserted) and keep every open conflicts-table entry that names them. The crew's check-offs on all four walls, made under GR-308, came across the retag (carry_floor2.mjs, 12 lines carried across a ruled retag floor-wide). The D30 mock-ups of 230 and 238 are superseded on this one line; the audit declares the difference. 53 checks pass. Still open: the per-room LEFT/RIGHT hand on every GR-305 and on GR-309 (D26: Austin is answering it). |
| D34 | 2026-09-02 | **FLOOR 3 IS BUILT OUT AND STAGED FOR APPROVAL - FF&E and MEP, not the 3D. Not live.** Austin: "once completed lets start floor 3 just no 3d BIM yet. i need this expedited so deploy as many agents and subagents that makes sense." Same generator as floor 2, generalised to a floor argument (build_floor2.mjs --floor=3, carry_floor2.mjs --floor=3, tests/floor3-audit.mjs, the book builder with --floor=3): 33 guest rooms, 66 documents, plus the three floor-3 common areas with a gated line (corridor 321 with an MEP companion, elevator lobby 337, ice machine 339); the other six spaces hold only finish rows outside the approved gate and get no document, listed. Floor 2 was regenerated under the generalised tool and re-audited (53 checks); only the wording of two build-authored room notes moved. **The crew's floor-3 work is carried exactly (D24): 412 check-offs, 701 open issues, 10 notes across 69 crew documents**, PII dropped, crew collection proven untouched; room 302's GR-905 line rebuilt from its own row so the crew's open issue has a home. Audit: 52 checks, all pass, including that every floor-3 room matches the staged floor-2 room of its type on shape apart from the floor-2 working-wall retags, that floor-3 lines cite floor-3 sheets (A102, A122) where floor 2 cites A101 and A121, and that floor2-staged.json was never written by a floor-3 run. **NO WORKING-WALL CORRECTION ON FLOOR 3, deliberately.** D22 and D33 rest on the workbook's 2nd Floor tab reconciling exactly against floor 2's key mix. The 3rd Floor tab prints the same six figures (GR-304 17, GR-305 11, GR-308 2, GR-309R 1, GR-315 1, GR-316 1) and floor 3's key mix per the drawings is different: nine plain Queen-Queen plus one QQ Wide plus two QQ Extended is 12 two-queen walls against 11 printed, and one connecting key (336) against 2 printed. GR-304, GR-309R, GR-315 and GR-316 reconcile; GR-305 and GR-308 do not, and D20 already found a copied-down figure on these tabs. So every two-queen key on floor 3 (301, 303, 305, 307, 309, 311, 313, 315, 328, 330, 332, 334, 338) carries GR-308 exactly as transcribed, FLAGGED, with the arithmetic in room note n_d22, OPEN for Austin: whether D22 and D33 carry up by room type, or whether the drawings (is 315 a connector on A102?) or the tab need correcting first. Review book: research/floor3/floor3book.html. Recorded before the fresh-eyes verifier reports; anything it finds is fixed on top. |
| D35 | 2026-09-02 | **On floor 3, D22 and D33 carry up by room type: plain Queen-Queen, QQ Wide and QQ Extended take GR-305; QQ Acc. takes GR-309; the connecting key keeps GR-308.** Austin, reviewing the floor-3 book: "carry D22 and D33 up by room type on floor 3". Applied to 301, 303, 305, 307, 309, 311, 313, 315, 328, 330, 332, 334 (GR-305) and 338 (GR-309); 336 stands on GR-308. The evidence on each line is the ruling itself: the workbook's 3rd Floor tab prints the same six working-wall figures as the 2nd (GR-305 11, GR-308 2) and floor 3 per the drawings has twelve non-connecting two-queen keys and one connector, so the tab is one GR-305 short and one GR-308 over and is quoted on the line as NOT the evidence; GR-304 (17), GR-309R (1), GR-315 (1) and GR-316 (1) do reconcile. Lines ship at MEDIUM with the hand open (5 LEFT / 6 RIGHT printed for eleven walls where floor 3 has twelve; the twelfth unit and every hand to be confirmed with RK Design). Carried as TAG_CORRECTIONS_F2 entries scoped to floor 3; the crew's check-offs made under GR-308 came across (9 lines carried across the retag). Two corrections made on the same pass, both floors rebuilt and re-audited (floor 2: 53 checks; floor 3: 55 checks): the conflicts-table room-key matcher no longer reads a tag run like "GR-300/305/307/308" as naming rooms 305, 307 and 308 (a fresh-eyes reconnaissance agent caught it; it had been misreporting on every floor since the mock-ups), and the generator's sprinkler comment no longer calls rooms 107 and 108 floor-2 keys. The recon agent independently confirmed the floor-3 room mix, the crew totals (412 / 701 / 10), the four space documents, the tab figures and the single gate-outside crew line (302 GR-905). Still open on floor 3: the hand per wall, the bathing configuration on 317 (whose drawing-record note says "roll-in bath drawn" while nobody has ruled) and 338, the common-area finish rows, and conflicts A11 (G001's matrix has no third-floor QQ accessible while the purchase record has one GR-309R). |
