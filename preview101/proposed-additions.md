# Room 101 — GR-305 / GR-320 / GR-325 identity hunt (ID set)

Source sheet: **ID-5.8** — "Enlarged Guestroom Plans & Elevations - QQ Std., QQ Ext., & QQ Std. Conn."
(FF&E Locations_sheet36.pdf, Drive `1TwllIXtTFD3Q-mGoV6jiTC4KK75iUS0U`; sheet ID read off the printed
title block, not inferred). All three tags are printed on the QQ FFE PLAN, right-wall callout column —
none of the three is on Austin's paper checklist or in the current template.
Evidence crops: `ev-GR-305-ID-5.8.png`, `ev-GR-320-325-ID-5.8.png`.

## GR-320 — IDENTIFIED, proposed as a new template line

- **Tag:** GR-320
- **Description:** Decorative Shelves above Desk
- **Sheet evidence:** ID-5.8 FFE plan tags GR-320 at the desk, stacked between GR-103 (task chair)
  and GR-201 (desk lamp); leader lands on a dashed (above-counter) shelf unit. Identity from the DB
  items table: five King packages carry `GR-320 · Decorative Shelves above Desk · A550:55 · HIGH` —
  same tag, same description, different room family.
- **Why it's missing today:** the QQ packages were built off A555, which does not tag GR-320;
  the RK ID sheet does. ID-5.8 vs A555 disagreement — classic FLAGGED territory.
- **Proposed line:** category `FF&E - Casegoods` · tag `GR-320` · "Decorative Shelves above Desk" ·
  qty 1 · reliability **FLAGGED** (tagged on ID-5.8 for the QQ plan; absent from A555 and from
  Austin's field sheet — confirm the QQ desk actually receives the shelves before ordering).

## GR-305 — IDENTIFIED, but do NOT add a line

- **Identity:** the FF&E spec reserves **GR-305 = Working Wall @ Queen Queen Studio Suite**
  (GR schedule via `ffe-guestroom.md` §4; DB conflict note OPEN_ITEMS A1).
- **Sheet evidence:** ID-5.8 FFE plan tags GR-305 on the right-wall working-wall run
  (between GR-403 closet drapery and GR-204 sconce) — see `ev-GR-305-ID-5.8.png`.
- **Why no new line:** the template already carries **GR-308 Working Wall @ QQ Connector**
  (FLAGGED, "one continuous run"). Spec says GR-305 = base QQ, GR-308 = connector; A555 tags
  GR-308, ID-5.8 tags both GR-308 (arch plan) and GR-305 (FFE plan). It is **one physical
  working wall per key** — adding GR-305 as a second line would double-count the same casework.
  GR-305 stays recorded as the alternate tag on the existing GR-308 FLAGGED row (which is exactly
  how the DB carries it).

## GR-325 — DEAD END, documented honestly

- ID-5.8 tags GR-325 above the desk (dashed overhead element above GR-103/GR-320 — see
  `ev-GR-320-325-ID-5.8.png`), so the tag is real and placed.
- But **no legend, schedule, or spec anywhere in the source set gives GR-325 a description**.
  `ffe-guestroom.md` rule 4 states verbatim: "GR-325, GR-326, GR-905 carry no description anywhere.
  Emitted FLAGGED, tag only. **Do not purchase.**" Gap G-10: with the FF&E Installation List
  excluded (OV-002), GR-325 has **no resolution path in the current source set**.
- The A530 FURNISHINGS LEGEND stops the casegood detail early (collapses GR-304–316 to one row)
  and the Hilton GR schedule has no 325 entry. Searched: ID set cover index (sheet 1), ID-5.8
  body text, DB items table (zero GR-325 rows).
- **Not proposed.** If Austin wants it chased, the fix is an RFI to RK Hospitality asking what
  GR-325 above the QQ desk is — nothing on hand answers it.
